# 개발/테스트 의존성. 앱 런타임 의존성(requirements.txt)이 먼저 설치돼 있어야 한다.
# BERT 검증(torch/transformers)은 테스트에서 목킹하므로 여기서는 불필요.
pytest>=8.0
# 무한 루프를 **매달리는 대신 떨어지게** 만든다. ERD 상속 순서잡기가 순환 밖에서
# 순환 안으로 들어가는 상속에서 안 끝난 적이 있는데, 순수 함수라 자체 타임아웃이 없어
# 테스트가 통째로 매달렸다. 회귀하면 CI가 멈추는 것이 아니라 실패해야 한다.
# 거는 곳은 tests/test_erd_mapping.py의 불변식 테스트뿐이다(전역 설정이 아니다).
pytest-timeout>=2.3
