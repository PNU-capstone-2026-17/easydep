# Secrets: real API key and database password live here.
.env

# Python
__pycache__/
*.py[cod]

# Generated artifacts and diagram render cache (rebuilt from MySQL on demand).
outputs/

# PlantUML tool, downloaded separately (17MB binary).
plantuml.jar

# Scratch files and report material, kept out of the project. Listed so they
# cannot be committed by accident if they reappear here.
error.puml
test.py
main.py
report/
