# DCL 스타일(generic description) CRD를 흉내낸 합성 fixture.
# - attachedRef: description으로는 대상을 알 수 없고 servicemappings로만 해석 가능 (tier 1)
# - firewallRef: servicemappings에 없음 → 필드명 휴리스틱 (tier 3)
# - widgetRef: 어느 방법으로도 해석 불가 → 엣지 생성 안 됨
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: computesyntheticthings.compute.cnrm.cloud.google.com
spec:
  group: compute.cnrm.cloud.google.com
  names:
    kind: ComputeSyntheticThing
    plural: computesyntheticthings
  scope: Namespaced
  versions:
    - name: v1beta1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              required:
                - attachedRef
              properties:
                attachedRef:
                  type: object
                  properties:
                    external:
                      description: The external name of the referenced resource
                      type: string
                    name:
                      description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                      type: string
                    namespace:
                      description: 'Namespace of the referent.'
                      type: string
                firewallRef:
                  type: object
                  properties:
                    external:
                      description: The external name of the referenced resource
                      type: string
                    name:
                      description: 'Name of the referent.'
                      type: string
                    namespace:
                      description: 'Namespace of the referent.'
                      type: string
                widgetRef:
                  type: object
                  properties:
                    external:
                      description: The external name of the referenced resource
                      type: string
                    name:
                      description: 'Name of the referent.'
                      type: string
                    namespace:
                      description: 'Namespace of the referent.'
                      type: string
