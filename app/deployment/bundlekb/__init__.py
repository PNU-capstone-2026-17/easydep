"""bundlekb: **무엇과 함께 만들어야 하나** — 리소스 군 지식베이스.

지식 차원 분담:

- `graphkb`    — 타입 간 의존성 (무엇이 무엇을 참조하나)
- `capacitykb` — 용량·제약 (무엇이 허용되나 / 한도 / 바꿀 수 있나)
- `costkb`     — 스펙·가격 (무엇을 살 수 있고 얼마인가)
- `perfkb`     — 성능 (그게 실제로 얼마나 빠른가)
- `bundlekb`   — 이 패키지 (**하나를 고르면 무엇이 따라오나**)

## 왜 graphkb로 안 되나

`research.md` 문제 2가 요구하는 것은 이렇다.

> *"특정 리소스를 선택하는 경우 **연계되는 다양한 리소스 군을 획득**할 수 있어야"*

graphkb는 스키마의 참조 관계를 따라간다. 그래서 `AWS::EC2::Instance`에서
`KMS::ReplicaKey`·`S3Tables::Table`까지 이어 준다 — **가능하지만 아무도 그렇게 안
쓴다.** `required_only`가 조금 줄일 뿐이고, 23개 노드에서 사이클 경고까지 뜬다.

**"가능한 것"과 "실제로 필요한 것"을 가르는 것**이 이 패키지의 존재 이유다.

## 세 층으로 가른다

번들의 구성원은 등급이 다르다. 섞으면 graphkb와 같은 실패가 된다.

    always     이 번들을 쓰면 **반드시** 만들어진다
    required   **값을 반드시 줘야 한다** (안 주면 배포가 안 된다)
    optional   붙일 수 있다 — 이게 곧 "연계되는 다양한 리소스 군"의 메뉴다

## 두 종류의 근거가 서로를 검증한다

    명시(stated)    AVM 모듈 구조 · tumblebug 코드 · 큐레이션 템플릿 · AWS 패턴
    실측(observed)  실제 템플릿 코퍼스의 동시 출현 비율

실측으로 확인한 것: Azure VM이 있는 템플릿 330개 중 **100.0%**에 네트워크 인터페이스가
있고, AVM도 독립적으로 그것을 유일한 필수 동반자로 짚는다. **둘이 일치할 때만** 강한
등급(`bundle-cross-checked`)을 준다 — `aws-cross-checked`와 같은 규율이다.

## 정직한 한계 (설계에 박아둘 것)

- **`observed`는 코퍼스의 사실이지 클라우드의 사실이 아니다.** 표본이 기울면 값도
  기운다 — VM과 스토리지 계정이 53.6%로 같이 나오는 건 옛 부트 진단 관행의 흔적이다.
- **표본이 적으면 비율이 소음이다.** 17개짜리 앵커의 35.3%는 6건이다. 최소 표본 수
  아래는 담지 않는다.
- **명시 소스도 "API가 강제한다"는 뜻이 아니다.** AVM은 모듈 저자의 설계이고,
  tumblebug 번들은 **우리 실행 경로**가 만드는 것이다. `avm-dependson`·`tpg-schema`에
  적어 둔 것과 같은 경계다.
"""
