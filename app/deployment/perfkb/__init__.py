"""perfkb: 클라우드 인스턴스 성능 특성 지식베이스.

"이 인스턴스가 **광고된 스펙만큼 실제로 일하나**" — 버스트 여부, 세대, 클럭,
EBS baseline 대역폭, Azure ACU 등.

지식 차원 분담:
- `graphkb`    — 타입 간 의존성 (무엇이 무엇을 필요로 하나)
- `capacitykb` — 용량·제약 (무엇이 허용되나 / 한도 / 바꿀 수 있나)
- `costkb`     — 스펙·가격 (무엇을 살 수 있고 얼마인가)
- `perfkb`     — 이 패키지 (그게 실제로 얼마나 빠른가)

## 왜 costkb에서 갈라져 나왔나

costkb만으로는 **가이드라인이 아니라 가격표**다. 실측: "웹서버급(vCPU 2, 메모리 4GiB)
최저가, AWS"를 물으면 상위 5개가 **전부 t3a.medium — 버스트 인스턴스**다. 상시 부하
API 서버에 그걸 1순위로 주는 건 싼 게 아니라 **틀린 가이드**다. 최저가 100개 중 37건이
버스트·구세대·공유CPU이고, AWS 인스턴스의 11.2%가 이미 구세대다.

`research.md`가 "클라우드 인스턴스 **성능**, 비용 등"을 나란히 쓴 이유가 이것이다.

## 소스 — costkb와 같은 행, 버렸던 컬럼

costkb 미러를 만들 때 `spec_infos`의 `details` 컬럼을 버렸다. 거기에 CSP 원본 응답이
**73,083행 100% 채움**으로 들어 있다(AWS는 `describe-instance-types` 그 자체).
그래서 새 소스가 필요 없고, costkb와 같은 미러 원칙이 그대로 적용된다.

**조인 키는 `id`** (`aws+us-east-1+t3.medium`) — costkb 레코드와 1:1이다.
단 조인은 **도구 계층(`nim_agent/`)에서** 한다. KB끼리는 서로 import하지 않는다.

## 정직한 한계 (설계에 박아둘 것)

- **프로바이더 간 성능 비교는 불가능하다.** ACU는 Azure에만, 클럭은 AWS에만 있다.
  "AWS m5.large vs Azure D2s_v3 중 뭐가 빨라?"는 **답할 수 없다고 말해야** 한다.
- **Azure ACU는 37.7%만 있고 결측이 설명되지 않는다**(세대별로 산발적). 없으면
  "느리다"가 아니라 "모른다"다.
- 상시 성능 판정은 프로바이더마다 **다른 개념**에서 온다 — `parsers/project.py` 참고.
"""

from __future__ import annotations

__version__ = "0.1.0"
