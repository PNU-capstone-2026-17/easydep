"""patternkb — 설계 패턴·원칙 **산문**의 검색 (advisory 전용).

## 다른 축과 무엇이 다른가

지금까지의 축은 전부 값·관계·제약(사실 후보)을 담았다. 이 축이 담는 것은 **산문**이다
— Azure Architecture Center의 클라우드 설계 패턴·아키텍처 스타일·설계 원칙(CC-BY-4.0)과
12factor 배포 원칙(MIT). 산문은 검색해서 **인용**할 수 있을 뿐, 어떤 가공을 해도
클라우드 사실이 되지 않는다. 그래서:

- 모든 답에 **"설계 지침이지 클라우드 사실이 아닙니다"**가 붙는다(`agent_api`).
- 근거 라벨은 `pattern-advisory` 하나이고 basis는 영원히 `inferred`다 — 검수해도
  사실이 되지 않는 성격이라 reviewed를 붙이지 않는 것이 규약이다.
- 값·한도·판정의 근거로 쓰면 안 된다. 그건 kb_*/cap_*/cost_* 축의 일이다.

## RAG는 벡터가 아니다

색인은 **SQLite FTS5**(표준 라이브러리·결정론·의존성 0)다. 재현율 부족이 **실측되면**
그때 임베딩으로 올린다 — "RAG = 벡터"라는 고정관념부터 버리는 게 이 저장소답다
(app-layer-plan 2026-07-23).

## 라이선스

코퍼스를 저장소에 커밋한다(`data/pattern-corpus.json.gz`). CC-BY-4.0은 재배포를
허용하되 **저작자 표시가 필수**라 `NOTICE`에 밝히고, 문서마다 `attribution` 칸을
데이터에 함께 담는다 — 파일 하나만 떼어 봐도 출처가 남게.
"""
