{
 "csp": "aws",
 "anchors": [
  "loadBalancer",
  "vm"
 ],
 "mapped": {
  "app": "vm",
  "securitygroup": "firewall",
  "sshkey": "sshKey",
  "subnet": "subnet",
  "vnet": "network",
  "app-lb": "loadBalancer"
 },
 "unmapped": {
  "app-db": "aws::AWS::RDS::DBInstance — 이 타입에 대응하는 실측 어휘가 없다",
  "secret-store": "aws::AWS::SecretsManager::Secret — 이 타입에 대응하는 실측 어휘가 없다",
  "app-files": "aws::AWS::S3::Bucket — 이 타입에 대응하는 실측 어휘가 없다"
 },
 "weak": {},
 "counts": {
  "out-of-scope": 3
 },
 "findings": [
  {
   "kind": "out-of-scope",
   "subject": "app-db",
   "observed": "aws::AWS::RDS::DBInstance — 이 타입에 대응하는 실측 어휘가 없다",
   "measured": "[managed-service] 관리형 상품 서비스(관계형 DB·큐·오브젝트 저장소·시크릿 저장소·검색·캐시 …). 상품이 CSP마다 수십 종이고 계속 늘어나 **실측으로는 따라갈 수 없다**. 계획은 이것들을 노드로 그리지만 우리는 그 의존에 대해 아무 말도 하지 않는다 — 침묵을 '문제없다'로 읽지 말 것"
  },
  {
   "kind": "out-of-scope",
   "subject": "app-files",
   "observed": "aws::AWS::S3::Bucket — 이 타입에 대응하는 실측 어휘가 없다",
   "measured": "[managed-service] 관리형 상품 서비스(관계형 DB·큐·오브젝트 저장소·시크릿 저장소·검색·캐시 …). 상품이 CSP마다 수십 종이고 계속 늘어나 **실측으로는 따라갈 수 없다**. 계획은 이것들을 노드로 그리지만 우리는 그 의존에 대해 아무 말도 하지 않는다 — 침묵을 '문제없다'로 읽지 말 것"
  },
  {
   "kind": "out-of-scope",
   "subject": "secret-store",
   "observed": "aws::AWS::SecretsManager::Secret — 이 타입에 대응하는 실측 어휘가 없다",
   "measured": "[managed-service] 관리형 상품 서비스(관계형 DB·큐·오브젝트 저장소·시크릿 저장소·검색·캐시 …). 상품이 CSP마다 수십 종이고 계속 늘어나 **실측으로는 따라갈 수 없다**. 계획은 이것들을 노드로 그리지만 우리는 그 의존에 대해 아무 말도 하지 않는다 — 침묵을 '문제없다'로 읽지 말 것"
  }
 ]
}