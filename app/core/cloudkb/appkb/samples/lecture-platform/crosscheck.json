{
 "csp": "aws",
 "anchors": [
  "loadBalancer",
  "vm"
 ],
 "mapped": {
  "app": "vm",
  "securitygroup": "firewall",
  "sshkey": "sshKey",
  "subnet": "subnet",
  "vnet": "network",
  "app-lb": "loadBalancer"
 },
 "unmapped": {
  "app-db": "aws::AWS::RDS::DBInstance — 이 타입에 대응하는 실측 어휘가 없다",
  "secret-store": "aws::AWS::SecretsManager::Secret — 이 타입에 대응하는 실측 어휘가 없다",
  "app-files": "aws::AWS::S3::Bucket — 이 타입에 대응하는 실측 어휘가 없다"
 },
 "weak": {
  "app": "VM"
 },
 "counts": {
  "out-of-vocabulary": 3,
  "weak-reading": 1,
  "missing-required": 1,
  "redundant-node": 1,
  "unchecked-rule": 2,
  "absent-order": 2,
  "absent-warning": 5,
  "absent-wait": 2
 },
 "findings": [
  {
   "kind": "out-of-vocabulary",
   "subject": "app-db",
   "observed": "aws::AWS::RDS::DBInstance — 이 타입에 대응하는 실측 어휘가 없다",
   "measured": "이 노드에 대해서는 아무 주장도 없다 — **문제없다는 뜻이 아니다**"
  },
  {
   "kind": "out-of-vocabulary",
   "subject": "app-files",
   "observed": "aws::AWS::S3::Bucket — 이 타입에 대응하는 실측 어휘가 없다",
   "measured": "이 노드에 대해서는 아무 주장도 없다 — **문제없다는 뜻이 아니다**"
  },
  {
   "kind": "out-of-vocabulary",
   "subject": "secret-store",
   "observed": "aws::AWS::SecretsManager::Secret — 이 타입에 대응하는 실측 어휘가 없다",
   "measured": "이 노드에 대해서는 아무 주장도 없다 — **문제없다는 뜻이 아니다**"
  },
  {
   "kind": "weak-reading",
   "subject": "app",
   "observed": "실행 방식이 표시 문자열 'VM'로만 있다 — 벤더 타입이 없다",
   "measured": "vm로 읽었다. 저쪽 문구가 바뀌면 이 대조가 조용히 끊긴다 — 계획이 컴퓨트 노드에 타입을 달면 강한 결속이 된다"
  },
  {
   "kind": "missing-required",
   "subject": "image",
   "observed": "이 자원이 계획에 없다",
   "measured": "aws에서 loadBalancer, vm를 만들려면 필수다(생성 거부로 실측)"
  },
  {
   "kind": "redundant-node",
   "subject": "firewall",
   "observed": "노드 `securitygroup`로 그려져 있다",
   "measured": "firewall을(를) 정하지 않으면 aws가 기본값으로 채웁니다 — 정하는 것 자체는 정상일 수 있다. 다만 계획이 **정했다는 사실**을 어디에도 안 적는다"
  },
  {
   "kind": "unchecked-rule",
   "subject": "customImage→vm",
   "observed": "계획의 관측: {'vm': 1} — 계획에 이 규칙을 적용한 흔적이 없다",
   "measured": "[카디널리티] AMI는 디스크가 아니라 **인스턴스**에서 나온다(create-image)"
  },
  {
   "kind": "unchecked-rule",
   "subject": "loadBalancer→subnet",
   "observed": "계획의 관측: {'loadBalancer': 1, 'subnet': 1} — 계획에 이 규칙을 적용한 흔적이 없다",
   "measured": "[카디널리티] ALB는 서로 다른 AZ의 서브넷 ≥2 (NLB는 1)"
  },
  {
   "kind": "absent-order",
   "subject": "생성 순서",
   "observed": "계획은 노드와 선만 담는다 — 순서를 담을 자리가 없다",
   "measured": "network → subnet → loadBalancer → vm"
  },
  {
   "kind": "absent-order",
   "subject": "삭제 순서",
   "observed": "계획에 삭제 순서가 없다 — 그대로 지우면 거부를 만난다",
   "measured": "subnet 먼저, 그다음 network"
  },
  {
   "kind": "absent-warning",
   "subject": "subnet→internetGateway",
   "observed": "계획에 이 경고가 없다",
   "measured": "internetGateway를 떼어도 subnet는 남지만 기능이 깨집니다 (컨트롤 플레인이 막지 않습니다 — 실측)"
  },
  {
   "kind": "absent-warning",
   "subject": "subnet→internetGateway",
   "observed": "계획에 이 경고가 없다",
   "measured": "internetGateway를 떼어도 subnet는 남지만 기능이 깨집니다 (컨트롤 플레인이 막지 않습니다 — 실측)"
  },
  {
   "kind": "absent-warning",
   "subject": "vm→firewall",
   "observed": "계획에 이 경고가 없다",
   "measured": "firewall를 떼어도 vm는 남지만 기능이 깨집니다 (컨트롤 플레인이 막지 않습니다 — 실측)"
  },
  {
   "kind": "absent-warning",
   "subject": "vm→iamRole",
   "observed": "계획에 이 경고가 없다",
   "measured": "iamRole를 떼어도 vm는 남지만 기능이 깨집니다 (컨트롤 플레인이 막지 않습니다 — 실측)"
  },
  {
   "kind": "absent-warning",
   "subject": "vm→publicIp",
   "observed": "계획에 이 경고가 없다",
   "measured": "publicIp를 떼어도 vm는 남지만 기능이 깨집니다 (컨트롤 플레인이 막지 않습니다 — 실측)"
  },
  {
   "kind": "absent-wait",
   "subject": "vm.create",
   "observed": "계획에 완료 대기가 없다",
   "measured": "State.Name == running (async-confirmed)"
  },
  {
   "kind": "absent-wait",
   "subject": "vm.delete",
   "observed": "계획에 완료 대기가 없다",
   "measured": "State.Name == terminated (async-confirmed)"
  }
 ]
}