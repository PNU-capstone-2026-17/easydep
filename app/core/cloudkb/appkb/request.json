{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "RESOURCE_SPEC v3 — 클라우드 리소스 제약 계약",
  "description": "**2026-08-01 판 3 — containerRegistry 신설.** 사슬을 끝까지 돌려 보니 매니페스트에 `image: <acr-name>.azurecr.io/app:<tag>` 자리표시자가 그대로 나갔다. 레지스트리 이름은 **사용자만 아는 값**이고 없으면 배포할 수 없는 YAML이 된다. 필수가 느는 방향이라 판을 올린다. || **2026-08-01 판 2 — 위상축 신설.** 계약을 제로베이스에서 다시 물었더니 축이 하나뿐이었다: 15칸이 전부 '어떻게 고르나'(조인·규모·정책)이고 '무엇을 놓나'가 없었다. 그래서 3사 실측 의존 주장 118건이 사슬에 진입할 입구가 없었다(앵커 후보는 CSP당 13~14종인데 받을 칸이 없었다). workloads를 필수로 신설한다 — **필수가 느는 방향이라 기존 명세가 전부 무효가 되므로 판을 2로 올린다**(판 1에서 규모 신호를 내릴 때 판을 유지한 것과 반대 방향이다). 그리고 이 파일이 산문으로 적어 온 규율 — '모든 칸에는 소비자가 있어야 한다' — 은 app/core/input_registry.py에서 항목의 칸(`opens`)이 되어 기계가 지킨다. 질문 문구·근거 좌표·계층·선행조건도 거기 있다. || 사용자 첫 요청의 클라우드 리소스 제약을 구조화한 것. easydep에서 요구사항 분석이 resource_constraints_text(원문 산문)로부터 만들어 RESOURCE_SPEC 산출물로 저장하고, 배포 구성이 읽는다. 필수의 기준은 목록이 아니라 판정식이다 — 그 칸이 없으면 뒤 단계 산출물의 요구사항 부합을 잴 수 없는 것만 필수다. **2026-07-29 재판정**: 이 판정식을 전 칸에 다시 걸어 규모 신호(expectedConcurrentUsers·approxRequestsPerSecond)를 필수에서 내렸다 — 그것으로 서는 판정이 하나도 없었기 때문이다(스펙 하한은 minVCpu·minMemoryGiB가 준다). 필수가 줄어드는 방향이라 기존 명세는 전부 유효해서 schemaVersion은 그대로 1이다. 필수는 아니지만 없으면 판정이 하나 닫히는 칸은 contract.SUGGESTED_WHY에 있고, 요구사항 에이전트가 그것을 '권고 질문'으로 함께 낸다. 그리고 모든 칸에는 소비자(어느 조인·어느 판정이 읽는가)가 있어야 한다 — multiZone을 받아 놓고 안 읽던 결함의 일반화. 소비자 없는 칸(데이터 볼륨·runtime·환경)은 여기 없고, 소비자가 생길 때 연다(dataResidency는 2층 보강에서 대조 소비자가 생겨 열렸다). **일부러 닫아 둔 칸**: availabilityTarget — 복제 수 판정의 근거(SLA)가 부재 확정이라(재조사 금지 목록) 소비자를 만들 수 없다. 규모→스펙 변환 계수 — 소스가 없다(사이징은 판단이지 사실이 아니다). 다시 열려면 그 소스부터.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "provider",
    "region",
    "workloads",
    "monthlyBudgetUSD",
    "containerRegistry"
  ],
  "properties": {
    "schemaVersion": {
      "const": "3"
    },
    "workloads": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string"
      },
      "description": "무엇을 배포하는가 — 앵커 자원 id의 목록(예: [\"k8sCluster\"], [\"vm\",\"loadBalancer\"]). **2026-08-01 신설**: 그전까지 계약에는 축이 하나뿐이었다(전부 '어떻게 고르나'이고 '무엇을 놓나'가 없었다). 소비자: app/core/infra_planning.plan_for_anchors — 폐포·생성 순서·삭제 순서·서버 합성·운영 경고가 전부 이 값에서 나온다. 없으면 3사 실측 주장이 계획에 닿지 못한다. 받는 값은 CSP마다 다르고 claims.json에서 나온다(input_registry.anchors_for) — 스키마가 enum으로 못 박지 않는 이유가 그것이다. **개수(같은 종류를 몇 개 놓는가)는 담지 않는다** — 우리 주장은 종류 수준이고 카디널리티는 실측한 적이 없다."
    },
    "provider": {
      "type": "string",
      "description": "aws | azure | gcp | … 값 조인의 축. 실측된 라우팅 변수 — 없으면 비용·성능·번들·벤더 타입 대응이 전부 닫힌다."
    },
    "region": {
      "type": "string",
      "description": "리전 코드(예: ap-northeast-2). 지명('서울')은 계약 진입 전에 리전 해석 도구(cap_resolve_region)로 코드로 바꿔 담는다 — 지명이 그대로 오면 조인이 조용히 빈 답이 된다(실측). 원문은 regionAsWritten에 남긴다."
    },
    "regionAsWritten": {
      "type": "string",
      "description": "사용자가 쓴 리전 표현 원문('서울' 등). 해석이 틀렸을 때 되짚을 근거 — REFINE_REQ가 원문을 남기는 것과 같은 이유다."
    },
    "monthlyBudgetUSD": {
      "type": "number",
      "exclusiveMinimum": 0,
      "description": "월 예산, USD. 비용 부합 판정의 기준값. 다른 통화는 **진입에서 USD로 환산해 담는다** — 요구사항 에이전트에 환율 도구(convert_to_usd, ECB 기준환율)가 있고, 쓴 환율과 기준일을 근거에 남긴다. (2026-07-29 정정: 예전 문구는 '환율 환산은 하지 않는다(환율 소스가 없다)'였는데, 그건 계약의 뜻이 아니라 그때 우리에게 소스가 없었다는 뜻이었다. 없는 능력은 거부할 것이 아니라 쥐어 줄 것이다.) 판정은 비대칭이다: 값이 붙는 부분의 하한만으로 초과는 확정할 수 있지만, 미가격 구성원이 있어 부합은 단정하지 못한다."
    },
    "expectedConcurrentUsers": {
      "type": "integer",
      "minimum": 1,
      "description": "예상 동시 사용자. **필수가 아니다**(2026-07-29 재판정 — 이것으로 서는 판정이 없다). **이 값은 스펙 하한을 정하지 않는다** — 동시 사용자를 vCPU·메모리로 바꾸는 변환은 1차 소스가 없어 KB에서 명시적으로 배제했다(조사 2026-07-22, 재확인 2026-07-29). 소비자 둘: (1) 하한이 없을 때 되묻기의 근거 — '동시 사용자 3,000인데 하한이 없다'가 '하한을 달라'보다 강한 요청이다. (2) 배포 계획이 추정임을 밝히며 인용하는 규모 진술. 하한 자체는 minVCpu·minMemoryGiB가 준다."
    },
    "approxRequestsPerSecond": {
      "type": "number",
      "exclusiveMinimum": 0,
      "description": "대략적 초당 요청 수. expectedConcurrentUsers의 대안 규모 신호이고, 소비자도 같다(하한이 아니라 되묻기의 근거)."
    },
    "minVCpu": {
      "type": "integer",
      "minimum": 1,
      "description": "이 워크로드가 요구하는 **최소 vCPU**. 사용자·설계자의 주장으로 기록된다(검증된 사실이 아니다 — stateless와 같은 성격). 소비자: 스펙 선택의 하한(층 2). 층 1은 sizingkb가 출처와 함께 들고 있는 하드 하한인데 실측상 k8s 노드에만 있어서, 일반 VM에는 이 칸이 유일한 하한이다. 없으면 **스펙을 고르지 않는다** — 최저가를 고르는 것은 중립이 아니라 '가장 작은 것으로 충분하다'는 근거 없는 주장이다."
    },
    "minMemoryGiB": {
      "type": "number",
      "exclusiveMinimum": 0,
      "description": "이 워크로드가 요구하는 최소 메모리(GiB). minVCpu와 같은 층·같은 성격이고 같은 소비자가 읽는다."
    },
    "multiZone": {
      "type": "boolean",
      "description": "가용영역 분산. tumblebug vNet 정책·k8s requiredSubnetCount 조인."
    },
    "trafficPattern": {
      "enum": [
        "steady",
        "spiky"
      ],
      "description": "부하 모양 — steady(상시 부하) | spiky(간헐 폭증). 소비자: 버스트 적합 판정 — steady인데 계획의 스펙에 버스트 경고(perfkb)가 붙어 있으면 상충으로 판정한다. 이 칸이 없으면 버스트 경고는 경고로만 남고 이 앱에 문제인지는 판정하지 않는다."
    },
    "stateless": {
      "type": "boolean",
      "description": "앱이 인스턴스(컴퓨트)에 상태를 두지 않는가 — 사용자·설계자의 주장으로 기록된다(검증된 사실이 아니다). 소비자: 서버리스 적합 판정 — false인데 서버리스 컴포넌트가 있으면 상충 가능성으로 판정한다."
    },
    "dataResidency": {
      "type": "string",
      "description": "데이터가 머물러야 하는 국가·지역, 사용자 표현 그대로(예: '한국', 'EU'). 소비자: 배포 계획이 리전의 **원본 표시 이름**(예: 'South Korea (Seoul)')을 대조 자료로 싣고, 부합 판정문은 **판정 불가를 명시**한다 — 리전의 국가를 기계 판정할 소스가 없다(표시 이름이 프로바이더마다 자유 서식 — 실측 2026-07-24). 산문에서 국가를 추출해 판정하면 확신에 찬 오답이 된다. 확인은 사용자 몫이고, 그 사실이 답에 실린다."
    },
    "lowCarbonPreferred": {
      "type": "boolean",
      "description": "요구사항이 에너지 사용·탄소 배출에 제약을 두는가. 소비자: 리전 탄소 판정 — 참인데 선택 리전보다 탄소집약도가 낮은 리전이 같은 프로바이더에 있으면 그 사실을 낸다. **판정이 아니라 대조다** — 탄소 수치의 출처 등급이 프로바이더마다 다르고(GCP는 구글 직접 발표, AWS·Azure는 서드파티 추정) 방법론이 달라 서로 비교할 수 없다. 이 칸이 없으면 탄소 데이터 161건은 질의응답에만 쓰이고 계획에 닿지 않는다."
    },
    "meta": {
      "type": "object"
    },
    "containerRegistry": {
      "type": "string",
      "description": "컨테이너 이미지를 올려 둘 레지스트리 이름. **2026-08-01 신설**: 사슬을 끝까지 돌려 보니 매니페스트에 `image: <acr-name>.azurecr.io/app:<tag>` 자리표시자가 그대로 나갔다 — 하류 렌더러(deployment_renderer:217)가 cloud 산출물에서 레지스트리 자원을 찾는데 아무도 안 내고 있었다. 이 값은 **사용자만 안다**(이미 있는 레지스트리를 쓰거나 만들어야 한다). 태그(`<tag>`)는 여기 없다 — 그건 CI가 정하는 것이고 요구사항 단계의 것이 아니다."
    }
  }
}
