# RWX PVC — 기본 SC(standard-rwo)로 RWX를 요구하면 어떻게 되는가.
# RWX 가능 SC 부재(1라운드 K4 관측)가 가설 — Pending과 이벤트가 관측 대상.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: depkb-synth2-rwx
spec:
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 5Gi
