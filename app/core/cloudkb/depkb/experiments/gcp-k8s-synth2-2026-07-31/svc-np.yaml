# Ingress 백엔드용 NodePort — 셀렉터 없는 무엔드포인트(합성은 건강과 별개라는
# 1라운드 관측 전제, 어긋나면 그대로 기록).
apiVersion: v1
kind: Service
metadata:
  name: depkb-synth2-np
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
