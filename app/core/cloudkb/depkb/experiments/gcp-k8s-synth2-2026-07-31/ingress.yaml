# Ingress — 기본 AKS에 컨트롤러가 없다는 가설의 관측 대상.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: depkb-synth2-ing
spec:
  defaultBackend:
    service:
      name: depkb-synth2-np
      port:
        number: 80
