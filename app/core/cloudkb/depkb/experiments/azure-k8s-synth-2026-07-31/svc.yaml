# type=LoadBalancer Service — 클라우드 LB 합성 여부가 측정 대상.
# 백엔드 상태는 판정과 무관하다(합성은 엔드포인트 건강과 별개).
apiVersion: v1
kind: Service
metadata:
  name: depkb-synth-svc
spec:
  type: LoadBalancer
  selector:
    app: depkb-synth
  ports:
    - port: 80
      targetPort: 80
