apiVersion: v1
kind: Pod
metadata:
  name: depkb-ekspvc3-pod
spec:
  containers:
    - name: pause
      image: registry.k8s.io/pause:3.9
      volumeMounts:
        - name: data
          mountPath: /data
  volumes:
    - name: data
      persistentVolumeClaim:
        claimName: depkb-ekspvc3
