# RWX PVC — 기본 EKS(gp2·EBS, RWX 불가)에서의 관측. 판정하지 않는다
# (노드 0·CSI 애드온 부재 — 1라운드와 같은 미측정 명시).
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: depkb-synth2-rwx
spec:
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 5Gi
