# type=LoadBalancer Service — 클라우드 LB 성좌 합성 여부가 측정 대상.
apiVersion: v1
kind: Service
metadata:
  name: depkb-synth-svc
spec:
  type: LoadBalancer
  selector:
    app: depkb-synth
  ports:
    - port: 80
      targetPort: 80
