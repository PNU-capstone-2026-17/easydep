"""depkb — 클라우드 리소스 의존의 제로베이스 지식.

2026-07-30 사용자 결정: **기존 산출물(graphkb·캐시·검수 파일)을 재료로 쓰지 않고
원천 수집부터 새로 시작한다.** 스파인은 벤더 스키마 원문이고, 어휘는 도구가 아니라
용도(하류가 정해야 하는 것)에서 온다. cloud-barista는 스파인이 아니다.

지식의 단위는 그래프가 아니라 **주장(claim)**이다:

    (주체 타입, 대상 타입, 질문, CSP, 술어)
      + 증거(원천 인용 목록) + 도달한 최고 오라클 층

- 질문 어휘는 existence·lifecycle 둘 — 기능(functional) 의존은 어떤 스키마도
  말하지 않으므로 명시적 공백이다.
- 오라클 서열: 컨트롤 플레인(apply) > preflight > 스키마 원문. **우리 산출물은
  오라클이 아니다** — 여기 있는 모든 것은 원천에서 재계산 가능해야 한다.
- CSP는 azure부터 — 동적 검증(계정)이 닿는 유일한 CSP라서다. aws·gcp는
  스키마 층까지만 먼저 선다.

graphkb(cb-tumblebug 전수)와의 관계: **독립이다.** 저쪽은 도구 층의 관측 코퍼스로
남고, 이쪽 주장의 근거로 쓰지 않는다.
"""
