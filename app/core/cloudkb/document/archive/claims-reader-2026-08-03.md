# `claims.json` 읽는 법 — 실제 값과 실험 추적표

> **불변 안내 스냅샷 (2026-08-03).** 이 문서는 현재 `claims.json`을 기계적으로 풀어 쓴 것이다. 새 결과를 만들 때는 `python -m app.core.cloudkb.depkb.build_claims` 뒤에 이 도구를 다시 실행한다.

## 1. 이 파일은 무엇인가

`claims.json`의 한 항목은 **한 CSP에서 한 자원 관계에 대해 한 질문을 던진 결과**다. 예를 들어 `gcp / fileSystem → network / existence / required`는 "GCP에서 파일 시스템을 만들 때 네트워크가 필수인가?"라는 질문의 결론이다.

이 파일은 실행 명령 모음이나 Cloud-Barista의 원본 데이터가 아니다. 이 저장소가 CSP 스키마와 직접 실험을 읽어 만든 분석 산출물이다. `experiment`와 `step`도 AWS·Azure·GCP가 정한 코드가 아니라 **이 저장소의 실험 작성자가 붙인 이름**이다.

## 2. 한 항목을 읽는 순서

1. `csp` — 어느 클라우드에서 얻은 결론인지 본다.
2. `subject` → `object` — **subject를 만들거나 작동시킬 때 object를 보는 관계**다.
3. `question` — 생성, 삭제/정리, 기능 중 무엇을 물었는지 본다.
4. `verdict` — 필수·선택·관계 확인 중 무엇인지 읽는다.
5. `predicate` — 그 결론이 성립하는 조건이다. 없으면 `별도 조건 없음`이다.
6. `evidence` — 그 결론을 재현할 수 있는 원천 위치다. 실험 ID를 보고 끝내지 말고 `resultFile`의 관측값과 `definition.file:line`의 요청 코드를 함께 본다.

## 3. 필드 사전

| 필드 | 실제 값의 예 | 뜻 | 어디를 보면 되나 |
|---|---|---|---|
| `subject` | `vm` | 관계의 주체. 이 자원을 만들거나 기능시키는 쪽 | `depkb/vocabulary.py`, claim |
| `object` | `nic` | 주체가 요구·선택·참조·기능상 의존하는 대상 | claim |
| `csp` | `azure` | 이 결론이 관측된 클라우드. 다른 CSP에 그대로 일반화하면 안 됨 | claim |
| `question` | `existence` | `existence`=생성 가능 여부, `lifecycle`=삭제/정리 관계, `function`=실제 기능 영향 | `closure.py`, claim |
| `verdict` | `required` | `required`=없으면 생성 불가, `optional`=없어도 생성 가능, `holds`=삭제/기능 관계를 관측, `unknown`=미확인 | `build_claims.py` |
| `predicate` | `배치 조건: ...` | 위 결론을 제한하는 조건. 예: 서로 다른 AZ, 둘 중 하나만 선택, 서버 자동 생성 | claim |
| `constraint` | `{"minCount": 2}` | 조건 중 기계가 검사할 수 있게 구조화한 값. 없으면 `null` | `infra_intent.py`, `check.py` |
| `signal` | `inbound-tcp` | 기능 의존을 무엇으로 측정했는지. 생성/삭제 claim에는 `null` | `build_claims.py:SIGNALS` |
| `oracle` | `apply` | 가장 강한 증거 층. `schema`=후보, `preflight`=사전검증, `apply`=실제 제어면 호출 | `build_claims.py` |
| `note` | 평이한 결론 문장 | 사람이 먼저 읽을 요약. 판정·조건·기능 신호를 다시 풀어 쓴 것 | claim |
| `evidence` | 배열 | 위 결론을 뒷받침하는 원천. 스키마 근거와 실험 근거가 섞일 수 있음 | 아래 §6 |

## 4. `evidence`의 필드와 `A2.dangling-network`의 정체

| 필드 | 뜻 |
|---|---|
| `layer` | `schema`, `preflight`, `apply` 중 증거의 층. 실제 API 호출인 `apply`가 가장 강하다. |
| `experiment` | 예: `gcp-fs-2026-07-31`. 이 저장소의 `depkb/experiments/` 아래 실험 폴더 이름이다. CSP가 발급한 ID가 아니다. |
| `step` | 예: `A2.dangling-network`. 그 실험의 `run.py`에서 연구자가 붙인 단계 이름이다. 앞 `A2`는 **그 실험 안의 순번**이며 전역 약어 표준이 아니다. |
| `code` | 기대한 결과. `ok`는 성공, 그 외는 실험에서 확인하려던 CSP 오류 코드다. |
| `observed` | 위 `code`를 문장으로 풀어 쓴 관측 결과다. |
| `resultFile` | 실제 성공/실패·오류 코드·원문 발췌가 저장된 JSON 경로다. |
| `definition.file`, `definition.line` | `step` 이름과 요청을 정의한 이 저장소의 Python 코드 위치다. 이 위치가 **그 단계 이름의 뜻을 정하는 원천**이다. |
| `sourceKind` | 단계 이름이 실험 코드에 정의됐는지, 결과 JSON에만 남았는지 표시한다. |

### 요청에서 든 예: `A2.dangling-network`

이 값은 `gcp-fs-2026-07-31` 실험의 단계 이름이다. `A2` 자체에는 "두 번째 존재 의존" 같은 공통 규칙이 없다. `dangling-network`는 존재하지 않는 네트워크를 가리키는 파일시스템 요청을 뜻하도록 **이 저장소의 실험 코드가 붙인 이름**이다. 정확한 명령·입력값은 아래 §6에서 이 값의 `definition.file:line`을 열어 확인한다. `results.json`은 그 요청 뒤 CSP가 돌려준 결과를 보관한다.

## 5. 현재 claims 전체 목록

현재 산출물은 119개 claim이다: existence 72개, lifecycle 32개, function 15개.

| CSP | 관계 | 질문 | 판정 | 조건 | 사람이 읽는 결론 | 증거 수 |
|---|---|---|---|---|---|---:|
| aws | `customImage` → `vm` | `existence` | `required` | CSP별 원본 차이: AMI는 디스크가 아니라 인스턴스에서 나온다(create-image) | aws에서 `customImage`와 `vm`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: CSP별 원본 차이: AMI는 디스크가 아니라 인스턴스에서 나온다(create-image). | 3 |
| aws | `fileSystem` → `subnet` | `existence` | `required` | 중간 자원을 통한 조건: 파일시스템 자체는 네트워크 없이 서고, 접속점(mount target)이 서브넷을 요구한다 | aws에서 `fileSystem`와 `subnet`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 중간 자원을 통한 조건: 파일시스템 자체는 네트워크 없이 서고, 접속점(mount target)이 서브넷을 요구한다. | 4 |
| aws | `fileSystem` → `subnet` | `lifecycle` | `holds` | 별도 조건 없음 | aws에서 `fileSystem`와 `subnet`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 별도 조건 없음. | 2 |
| aws | `firewall` → `network` | `existence` | `optional` | 서버 기본값: 미지정 시 기본 VPC로 대체 | aws에서 `firewall`와 `network`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 서버 기본값: 미지정 시 기본 VPC로 대체. | 3 |
| aws | `globalDns` → `globalDnsRecord` | `existence` | `required` | 별도 조건 없음 | aws에서 `globalDns`와 `globalDnsRecord`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 별도 조건 없음. | 2 |
| aws | `globalDns` → `globalDnsRecord` | `lifecycle` | `holds` | 별도 조건 없음 | aws에서 `globalDns`와 `globalDnsRecord`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 별도 조건 없음. | 2 |
| aws | `globalDns` → `network` | `existence` | `required` | 스킴 조건: 사설 영역에서만 필수(공인 영역 미측정) | aws에서 `globalDns`와 `network`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 스킴 조건: 사설 영역에서만 필수(공인 영역 미측정). | 2 |
| aws | `k8sCluster` → `firewall` | `existence` | `optional` | 서버 자동 생성: 클러스터 보안 그룹을 서비스가 만든다 | aws에서 `k8sCluster`와 `firewall`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 서버 자동 생성: 클러스터 보안 그룹을 서비스가 만든다. | 1 |
| aws | `k8sCluster` → `iamRole` | `existence` | `required` | 별도 조건 없음 | aws에서 `k8sCluster`와 `iamRole`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 별도 조건 없음. | 3 |
| aws | `k8sCluster` → `iamRole` | `function` | `holds` | 기능상 주의: ACTIVE 클러스터의 역할에서 정책 분리를 컨트롤 플레인이 막지 않는다 — 기능 신호는 새 노드그룹이 터미널 상태(join)까지 가는가 | aws에서 `k8sCluster`와 `iamRole`의 기능 의존성(대상 변화로 실제 기능이 깨지는지)을 확인한 결과는 `관계 확인됨`이다. 조건: 기능상 주의: ACTIVE 클러스터의 역할에서 정책 분리를 컨트롤 플레인이 막지 않는다 — 기능 신호는 새 노드그룹이 터미널 상태(join)까지 가는가. 기능 확인 신호는 새 Kubernetes 노드 그룹이 클러스터에 합류하는지이다. | 6 |
| aws | `k8sCluster` → `network` | `lifecycle` | `holds` | 별도 조건 없음 | aws에서 `k8sCluster`와 `network`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 별도 조건 없음. | 2 |
| aws | `k8sCluster` → `subnet` | `existence` | `required` | 배치 조건: 서로 다른 AZ의 서브넷 ≥2 (양성 대조로 대우 확인) | aws에서 `k8sCluster`와 `subnet`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 배치 조건: 서로 다른 AZ의 서브넷 ≥2 (양성 대조로 대우 확인). | 6 |
| aws | `k8sCluster` → `subnet` | `lifecycle` | `holds` | 별도 조건 없음 | aws에서 `k8sCluster`와 `subnet`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 별도 조건 없음. | 2 |
| aws | `k8sIngress` → `loadBalancer` | `existence` | `optional` | 별도 조건 없음 | aws에서 `k8sIngress`와 `loadBalancer`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 별도 조건 없음. | 3 |
| aws | `k8sPvc` → `disk` | `existence` | `optional` | 서버 자동 생성: CSI가 EBS 볼륨을 합성한다 — 단 전제가 3사 중 가장 길다(노드그룹·CSI 드라이버·IRSA·CSI 프로비저너 StorageClass) | aws에서 `k8sPvc`와 `disk`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 서버 자동 생성: CSI가 EBS 볼륨을 합성한다 — 단 전제가 3사 중 가장 길다(노드그룹·CSI 드라이버·IRSA·CSI 프로비저너 StorageClass). | 6 |
| aws | `k8sPvc` → `disk` | `lifecycle` | `holds` | 자동 정리 조건: PVC 삭제가 합성 EBS 볼륨을 지운다 | aws에서 `k8sPvc`와 `disk`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 자동 정리 조건: PVC 삭제가 합성 EBS 볼륨을 지운다. | 2 |
| aws | `k8sService` → `loadBalancer` | `existence` | `optional` | 서버 자동 생성: type=LoadBalancer 서비스가 CLB와 전용 SG(k8s-elb-…)를 합성한다 — 노드 0에서도 | aws에서 `k8sService`와 `loadBalancer`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 서버 자동 생성: type=LoadBalancer 서비스가 CLB와 전용 SG(k8s-elb-…)를 합성한다 — 노드 0에서도. | 3 |
| aws | `k8sService` → `loadBalancer` | `lifecycle` | `holds` | 자동 정리 조건: Service 삭제가 CLB를 지운다 | aws에서 `k8sService`와 `loadBalancer`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 자동 정리 조건: Service 삭제가 CLB를 지운다. | 1 |
| aws | `loadBalancer` → `firewall` | `existence` | `optional` | 별도 조건 없음 | aws에서 `loadBalancer`와 `firewall`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 별도 조건 없음. | 2 |
| aws | `loadBalancer` → `subnet` | `existence` | `required` | 배치 조건: ALB는 서로 다른 AZ의 서브넷 ≥2 (NLB는 1) | aws에서 `loadBalancer`와 `subnet`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 배치 조건: ALB는 서로 다른 AZ의 서브넷 ≥2 (NLB는 1). | 5 |
| aws | `nic` → `firewall` | `existence` | `optional` | 서버 기본값: 미지정 시 VPC의 default SG 부착 | aws에서 `nic`와 `firewall`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 서버 기본값: 미지정 시 VPC의 default SG 부착. | 3 |
| aws | `nic` → `firewall` | `lifecycle` | `holds` | 별도 조건 없음 | aws에서 `nic`와 `firewall`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 별도 조건 없음. | 3 |
| aws | `nic` → `subnet` | `existence` | `required` | 별도 조건 없음 | aws에서 `nic`와 `subnet`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 별도 조건 없음. | 2 |
| aws | `nic` → `subnet` | `lifecycle` | `holds` | 별도 조건 없음 | aws에서 `nic`와 `subnet`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 별도 조건 없음. | 3 |
| aws | `subnet` → `internetGateway` | `function` | `holds` | 기능상 주의: 0.0.0.0/0→IGW 라우트 삭제를 막지 않는다 — 기능 신호는 게스트에서 밖으로 나가는 HTTPS(아웃바운드) | aws에서 `subnet`와 `internetGateway`의 기능 의존성(대상 변화로 실제 기능이 깨지는지)을 확인한 결과는 `관계 확인됨`이다. 조건: 기능상 주의: 0.0.0.0/0→IGW 라우트 삭제를 막지 않는다 — 기능 신호는 게스트에서 밖으로 나가는 HTTPS(아웃바운드). 기능 확인 신호는 인스턴스에서 외부 HTTPS로 나갈 수 있는지이다. | 5 |
| aws | `subnet` → `internetGateway` | `function` | `holds` | 기능상 주의: 라우트 테이블에서 0.0.0.0/0→IGW 라우트 삭제를 컨트롤 플레인이 막지 않는다 | aws에서 `subnet`와 `internetGateway`의 기능 의존성(대상 변화로 실제 기능이 깨지는지)을 확인한 결과는 `관계 확인됨`이다. 조건: 기능상 주의: 라우트 테이블에서 0.0.0.0/0→IGW 라우트 삭제를 컨트롤 플레인이 막지 않는다. 기능 확인 신호는 외부에서 TCP로 도달할 수 있는지이다. | 5 |
| aws | `subnet` → `network` | `existence` | `required` | 별도 조건 없음 | aws에서 `subnet`와 `network`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 별도 조건 없음. | 2 |
| aws | `subnet` → `network` | `lifecycle` | `holds` | 별도 조건 없음 | aws에서 `subnet`와 `network`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 별도 조건 없음. | 3 |
| aws | `vm` → `customImage` | `existence` | `optional` | 별도 조건 없음 | aws에서 `vm`와 `customImage`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 별도 조건 없음. | 2 |
| aws | `vm` → `disk` | `existence` | `optional` | 서버 자동 생성: AMI의 루트 볼륨을 서버가 만든다 | aws에서 `vm`와 `disk`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 서버 자동 생성: AMI의 루트 볼륨을 서버가 만든다. | 2 |
| aws | `vm` → `firewall` | `existence` | `optional` | 서버 기본값: 미지정 시 default SG | aws에서 `vm`와 `firewall`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 서버 기본값: 미지정 시 default SG. | 4 |
| aws | `vm` → `firewall` | `function` | `holds` | 기능상 주의: 실행 중 인스턴스의 SG를 빈 인그레스 SG로 교체하는 것을 컨트롤 플레인이 막지 않는다(관계 변이) | aws에서 `vm`와 `firewall`의 기능 의존성(대상 변화로 실제 기능이 깨지는지)을 확인한 결과는 `관계 확인됨`이다. 조건: 기능상 주의: 실행 중 인스턴스의 SG를 빈 인그레스 SG로 교체하는 것을 컨트롤 플레인이 막지 않는다(관계 변이). 기능 확인 신호는 외부에서 TCP로 도달할 수 있는지이다. | 8 |
| aws | `vm` → `iamRole` | `existence` | `optional` | 별도 조건 없음 | aws에서 `vm`와 `iamRole`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 별도 조건 없음. | 1 |
| aws | `vm` → `iamRole` | `function` | `holds` | 기능상 주의: 실행 중 인스턴스에서 인스턴스 프로필 분리를 컨트롤 플레인이 막지 않는다 — 기능 신호는 게스트의 IMDSv2 자격증명 획득 | aws에서 `vm`와 `iamRole`의 기능 의존성(대상 변화로 실제 기능이 깨지는지)을 확인한 결과는 `관계 확인됨`이다. 조건: 기능상 주의: 실행 중 인스턴스에서 인스턴스 프로필 분리를 컨트롤 플레인이 막지 않는다 — 기능 신호는 게스트의 IMDSv2 자격증명 획득. 기능 확인 신호는 인스턴스 메타데이터에서 자격 증명을 받는지이다. | 5 |
| aws | `vm` → `image` | `existence` | `required` | 별도 조건 없음 | aws에서 `vm`와 `image`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 별도 조건 없음. | 4 |
| aws | `vm` → `nic` | `existence` | `optional` | 서버 자동 생성: ENI를 서버가 암묵 생성 | aws에서 `vm`와 `nic`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 서버 자동 생성: ENI를 서버가 암묵 생성. | 2 |
| aws | `vm` → `publicIp` | `function` | `holds` | 기능상 주의: running 인스턴스에서 EIP 분리를 컨트롤 플레인이 막지 않는다 — 기능 신호는 외부 TCP 22 도달성 | aws에서 `vm`와 `publicIp`의 기능 의존성(대상 변화로 실제 기능이 깨지는지)을 확인한 결과는 `관계 확인됨`이다. 조건: 기능상 주의: running 인스턴스에서 EIP 분리를 컨트롤 플레인이 막지 않는다 — 기능 신호는 외부 TCP 22 도달성. 기능 확인 신호는 외부에서 TCP로 도달할 수 있는지이다. | 6 |
| aws | `vm` → `sshKey` | `existence` | `optional` | 별도 조건 없음 | aws에서 `vm`와 `sshKey`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 별도 조건 없음. | 3 |
| aws | `vm` → `subnet` | `existence` | `optional` | 서버 기본값: 미지정 시 기본 VPC 서브넷 대체 | aws에서 `vm`와 `subnet`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 서버 기본값: 미지정 시 기본 VPC 서브넷 대체. | 4 |
| aws | `vpn` → `network` | `existence` | `required` | 별도 조건 없음 | aws에서 `vpn`와 `network`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 별도 조건 없음. | 3 |
| aws | `vpn` → `network` | `lifecycle` | `holds` | 별도 조건 없음 | aws에서 `vpn`와 `network`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 별도 조건 없음. | 2 |
| azure | `customImage` → `disk` | `existence` | `required` | 별도 조건 없음 | azure에서 `customImage`와 `disk`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 별도 조건 없음. | 2 |
| azure | `fileSystem` → `storageAccount` | `existence` | `required` | 별도 조건 없음 | azure에서 `fileSystem`와 `storageAccount`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 별도 조건 없음. | 2 |
| azure | `globalDns` → `globalDnsRecord` | `existence` | `required` | 별도 조건 없음 | azure에서 `globalDns`와 `globalDnsRecord`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 별도 조건 없음. | 2 |
| azure | `globalDns` → `network` | `function` | `holds` | 기능상 주의: 사설 영역-vnet link 삭제를 컨트롤 플레인이 막지 않는다 — 기능 신호는 게스트의 이름 해석(getent hosts) | azure에서 `globalDns`와 `network`의 기능 의존성(대상 변화로 실제 기능이 깨지는지)을 확인한 결과는 `관계 확인됨`이다. 조건: 기능상 주의: 사설 영역-vnet link 삭제를 컨트롤 플레인이 막지 않는다 — 기능 신호는 게스트의 이름 해석(getent hosts). 기능 확인 신호는 인스턴스가 이름을 IP로 해석하는지이다. | 5 |
| azure | `k8sCluster` → `iamRole` | `existence` | `optional` | 서버 자동 생성: identity 미지정 시 SystemAssigned와 kubelet용 user-assigned identity를 서버가 합성한다 | azure에서 `k8sCluster`와 `iamRole`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 서버 자동 생성: identity 미지정 시 SystemAssigned와 kubelet용 user-assigned identity를 서버가 합성한다. | 1 |
| azure | `k8sCluster` → `k8sNodeGroup` | `existence` | `required` | 수명주기 조건: 생성 시 필수 · 이후 독립 CRUD(add/delete) | azure에서 `k8sCluster`와 `k8sNodeGroup`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 수명주기 조건: 생성 시 필수 · 이후 독립 CRUD(add/delete). | 3 |
| azure | `k8sCluster` → `network` | `lifecycle` | `holds` | 별도 조건 없음 | azure에서 `k8sCluster`와 `network`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 별도 조건 없음. | 2 |
| azure | `k8sCluster` → `subnet` | `existence` | `optional` | 서버 자동 생성: 서비스가 노드 리소스 그룹에 vnet을 합성 | azure에서 `k8sCluster`와 `subnet`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 서버 자동 생성: 서비스가 노드 리소스 그룹에 vnet을 합성. | 3 |
| azure | `k8sCluster` → `subnet` | `lifecycle` | `holds` | 별도 조건 없음 | azure에서 `k8sCluster`와 `subnet`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 별도 조건 없음. | 2 |
| azure | `k8sIngress` → `loadBalancer` | `existence` | `optional` | 별도 조건 없음 | azure에서 `k8sIngress`와 `loadBalancer`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 별도 조건 없음. | 3 |
| azure | `k8sPvc` → `disk` | `existence` | `optional` | 서버 자동 생성: CSI가 관리 디스크를 합성한다 — 단 첫 소비자(Pod) 시점(기본 SC WaitForFirstConsumer 실측) | azure에서 `k8sPvc`와 `disk`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 서버 자동 생성: CSI가 관리 디스크를 합성한다 — 단 첫 소비자(Pod) 시점(기본 SC WaitForFirstConsumer 실측). | 5 |
| azure | `k8sPvc` → `disk` | `lifecycle` | `holds` | 자동 정리 조건: PVC 삭제가 합성 디스크를 지운다(기본 reclaim Delete 실측) | azure에서 `k8sPvc`와 `disk`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 자동 정리 조건: PVC 삭제가 합성 디스크를 지운다(기본 reclaim Delete 실측). | 1 |
| azure | `k8sPvc` → `fileSystem` | `existence` | `optional` | 서버 자동 생성: RWX PVC는 CSI가 스토리지 계정과 파일 공유를 합성한다(azurefile-csi, bind=Immediate) | azure에서 `k8sPvc`와 `fileSystem`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 서버 자동 생성: RWX PVC는 CSI가 스토리지 계정과 파일 공유를 합성한다(azurefile-csi, bind=Immediate). | 4 |
| azure | `k8sPvc` → `fileSystem` | `lifecycle` | `holds` | 자동 정리 조건: PVC 삭제가 파일 공유를 지운다 — 단 스토리지 계정은 남는다(부분 정리) | azure에서 `k8sPvc`와 `fileSystem`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 자동 정리 조건: PVC 삭제가 파일 공유를 지운다 — 단 스토리지 계정은 남는다(부분 정리). | 2 |
| azure | `k8sService` → `loadBalancer` | `existence` | `optional` | 서버 자동 생성: type=LoadBalancer 서비스가 노드 RG에 클라우드 LB 규칙·공인 IP를 합성한다 | azure에서 `k8sService`와 `loadBalancer`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 서버 자동 생성: type=LoadBalancer 서비스가 노드 RG에 클라우드 LB 규칙·공인 IP를 합성한다. | 3 |
| azure | `k8sService` → `loadBalancer` | `lifecycle` | `holds` | 자동 정리 조건: Service 삭제가 LB 규칙과 합성 PIP를 함께 지운다 | azure에서 `k8sService`와 `loadBalancer`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 자동 정리 조건: Service 삭제가 LB 규칙과 합성 PIP를 함께 지운다. | 2 |
| azure | `loadBalancer` → `publicIp` | `existence` | `optional` | 별도 조건 없음 | azure에서 `loadBalancer`와 `publicIp`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 별도 조건 없음. | 2 |
| azure | `loadBalancer` → `subnet` | `existence` | `optional` | 별도 조건 없음 | azure에서 `loadBalancer`와 `subnet`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 별도 조건 없음. | 2 |
| azure | `loadBalancer` → `subnet\|publicIp\|publicIPPrefix` | `existence` | `required` | 선택 규칙: 셋 중 정확히 하나(배타적) | azure에서 `loadBalancer`와 `subnet\|publicIp\|publicIPPrefix`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 선택 규칙: 셋 중 정확히 하나(배타적). | 6 |
| azure | `loadBalancer` → `vm` | `function` | `holds` | 기능상 주의: 백엔드 풀에서 NIC 제거를 컨트롤 플레인이 막지 않는다 — 기능 신호는 LB 프론트엔드로의 HTTP 200 | azure에서 `loadBalancer`와 `vm`의 기능 의존성(대상 변화로 실제 기능이 깨지는지)을 확인한 결과는 `관계 확인됨`이다. 조건: 기능상 주의: 백엔드 풀에서 NIC 제거를 컨트롤 플레인이 막지 않는다 — 기능 신호는 LB 프론트엔드로의 HTTP 200. 기능 확인 신호는 로드밸런서 주소가 HTTP 200으로 응답하는지이다. | 6 |
| azure | `network` → `subnet` | `existence` | `optional` | 별도 조건 없음 | azure에서 `network`와 `subnet`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 별도 조건 없음. | 2 |
| azure | `nic` → `firewall` | `existence` | `optional` | 별도 조건 없음 | azure에서 `nic`와 `firewall`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 별도 조건 없음. | 2 |
| azure | `nic` → `firewall` | `lifecycle` | `holds` | 별도 조건 없음 | azure에서 `nic`와 `firewall`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 별도 조건 없음. | 3 |
| azure | `nic` → `publicIp` | `existence` | `optional` | 별도 조건 없음 | azure에서 `nic`와 `publicIp`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 별도 조건 없음. | 3 |
| azure | `nic` → `publicIp` | `function` | `holds` | 기능상 주의: 실행 중 VM의 NIC에서 PIP 분리를 컨트롤 플레인이 막지 않는다 — 기능 신호는 외부 TCP 22 도달성 | azure에서 `nic`와 `publicIp`의 기능 의존성(대상 변화로 실제 기능이 깨지는지)을 확인한 결과는 `관계 확인됨`이다. 조건: 기능상 주의: 실행 중 VM의 NIC에서 PIP 분리를 컨트롤 플레인이 막지 않는다 — 기능 신호는 외부 TCP 22 도달성. 기능 확인 신호는 외부에서 TCP로 도달할 수 있는지이다. | 8 |
| azure | `nic` → `publicIp` | `lifecycle` | `holds` | 별도 조건 없음 | azure에서 `nic`와 `publicIp`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 별도 조건 없음. | 4 |
| azure | `nic` → `subnet` | `existence` | `required` | 별도 조건 없음 | azure에서 `nic`와 `subnet`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 별도 조건 없음. | 5 |
| azure | `nic` → `subnet` | `lifecycle` | `holds` | 별도 조건 없음 | azure에서 `nic`와 `subnet`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 별도 조건 없음. | 5 |
| azure | `subnet` → `firewall` | `existence` | `optional` | 별도 조건 없음 | azure에서 `subnet`와 `firewall`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 별도 조건 없음. | 2 |
| azure | `subnet` → `firewall` | `function` | `holds` | 기능상 주의: 사용 중 서브넷에서 NSG 분리를 컨트롤 플레인이 막지 않는다 — 차단은 NSG 부재와 Standard PIP secure-by-default의 합성 효과 | azure에서 `subnet`와 `firewall`의 기능 의존성(대상 변화로 실제 기능이 깨지는지)을 확인한 결과는 `관계 확인됨`이다. 조건: 기능상 주의: 사용 중 서브넷에서 NSG 분리를 컨트롤 플레인이 막지 않는다 — 차단은 NSG 부재와 Standard PIP secure-by-default의 합성 효과. 기능 확인 신호는 외부에서 TCP로 도달할 수 있는지이다. | 6 |
| azure | `subnet` → `network` | `existence` | `required` | 별도 조건 없음 | azure에서 `subnet`와 `network`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 별도 조건 없음. | 2 |
| azure | `subnet` → `network` | `lifecycle` | `holds` | 별도 조건 없음 | azure에서 `subnet`와 `network`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 별도 조건 없음. | 3 |
| azure | `vm` → `customImage` | `existence` | `optional` | 별도 조건 없음 | azure에서 `vm`와 `customImage`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 별도 조건 없음. | 1 |
| azure | `vm` → `disk` | `existence` | `optional` | 별도 조건 없음 | azure에서 `vm`와 `disk`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 별도 조건 없음. | 3 |
| azure | `vm` → `disk` | `function` | `holds` | 기능상 주의: 실행 중 VM에서 데이터 디스크 detach를 컨트롤 플레인이 막지 않는다 — 기능 신호는 게스트의 direct 쓰기 | azure에서 `vm`와 `disk`의 기능 의존성(대상 변화로 실제 기능이 깨지는지)을 확인한 결과는 `관계 확인됨`이다. 조건: 기능상 주의: 실행 중 VM에서 데이터 디스크 detach를 컨트롤 플레인이 막지 않는다 — 기능 신호는 게스트의 direct 쓰기. 기능 확인 신호는 볼륨에 실제로 쓸 수 있는지이다. | 6 |
| azure | `vm` → `disk` | `lifecycle` | `holds` | 별도 조건 없음 | azure에서 `vm`와 `disk`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 별도 조건 없음. | 4 |
| azure | `vm` → `iamRole` | `existence` | `optional` | 별도 조건 없음 | azure에서 `vm`와 `iamRole`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 별도 조건 없음. | 1 |
| azure | `vm` → `image` | `existence` | `optional` | 선택 규칙: 부팅 원천 — imageReference ∨ 기존 OS 디스크 attach 중 정확히 하나(배타적) | azure에서 `vm`와 `image`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 선택 규칙: 부팅 원천 — imageReference ∨ 기존 OS 디스크 attach 중 정확히 하나(배타적). | 8 |
| azure | `vm` → `nic` | `existence` | `required` | 별도 조건 없음 | azure에서 `vm`와 `nic`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 별도 조건 없음. | 3 |
| azure | `vpn` → `publicIp` | `existence` | `required` | 두 자원의 호환 조건: AZ SKU 게이트웨이는 zone이 구성된 PIP를 요구한다 | azure에서 `vpn`와 `publicIp`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 두 자원의 호환 조건: AZ SKU 게이트웨이는 zone이 구성된 PIP를 요구한다. | 2 |
| azure | `vpn` → `publicIp` | `lifecycle` | `holds` | 별도 조건 없음 | azure에서 `vpn`와 `publicIp`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 별도 조건 없음. | 2 |
| azure | `vpn` → `subnet` | `existence` | `required` | 이름 조건: 정확히 GatewaySubnet이라는 서브넷이어야 한다 | azure에서 `vpn`와 `subnet`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 이름 조건: 정확히 GatewaySubnet이라는 서브넷이어야 한다. | 1 |
| azure | `vpn` → `subnet` | `lifecycle` | `holds` | 별도 조건 없음 | azure에서 `vpn`와 `subnet`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 별도 조건 없음. | 2 |
| gcp | `customImage` → `disk` | `existence` | `required` | 별도 조건 없음 | gcp에서 `customImage`와 `disk`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 별도 조건 없음. | 2 |
| gcp | `fileSystem` → `network` | `existence` | `required` | 별도 조건 없음 | gcp에서 `fileSystem`와 `network`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 별도 조건 없음. | 4 |
| gcp | `firewall` → `network` | `existence` | `optional` | 서버 기본값: 미지정 시 default 네트워크로 대체 | gcp에서 `firewall`와 `network`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 서버 기본값: 미지정 시 default 네트워크로 대체. | 3 |
| gcp | `globalDns` → `globalDnsRecord` | `existence` | `required` | 별도 조건 없음 | gcp에서 `globalDns`와 `globalDnsRecord`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 별도 조건 없음. | 2 |
| gcp | `globalDns` → `globalDnsRecord` | `lifecycle` | `holds` | 별도 조건 없음 | gcp에서 `globalDns`와 `globalDnsRecord`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 별도 조건 없음. | 2 |
| gcp | `k8sCluster` → `k8sNodeGroup` | `existence` | `optional` | 서버 자동 생성: 미지정 시 default-pool 합성 · 이후 add/delete 가능 | gcp에서 `k8sCluster`와 `k8sNodeGroup`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 서버 자동 생성: 미지정 시 default-pool 합성 · 이후 add/delete 가능. | 3 |
| gcp | `k8sCluster` → `network` | `existence` | `optional` | 서버 기본값: 미지정 시 default 네트워크 | gcp에서 `k8sCluster`와 `network`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 서버 기본값: 미지정 시 default 네트워크. | 3 |
| gcp | `k8sCluster` → `network` | `lifecycle` | `holds` | 별도 조건 없음 | gcp에서 `k8sCluster`와 `network`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 별도 조건 없음. | 2 |
| gcp | `k8sCluster` → `subnet` | `existence` | `optional` | 서버 기본값: 미지정 시 default 서브넷 | gcp에서 `k8sCluster`와 `subnet`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 서버 기본값: 미지정 시 default 서브넷. | 2 |
| gcp | `k8sCluster` → `subnet` | `lifecycle` | `holds` | 별도 조건 없음 | gcp에서 `k8sCluster`와 `subnet`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 별도 조건 없음. | 2 |
| gcp | `k8sIngress` → `loadBalancer` | `existence` | `optional` | 서버 자동 생성: 내장 컨트롤러가 전역 HTTP LB 성좌(urlMap·targetHttpProxy·전역 forwardingRule·backendService·healthCheck)를 합성한다 | gcp에서 `k8sIngress`와 `loadBalancer`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 서버 자동 생성: 내장 컨트롤러가 전역 HTTP LB 성좌(urlMap·targetHttpProxy·전역 forwardingRule·backendService·healthCheck)를 합성한다. | 3 |
| gcp | `k8sIngress` → `loadBalancer` | `lifecycle` | `holds` | 자동 정리 조건: Ingress 삭제가 전역 성좌 전체를 함께 지운다 | gcp에서 `k8sIngress`와 `loadBalancer`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 자동 정리 조건: Ingress 삭제가 전역 성좌 전체를 함께 지운다. | 1 |
| gcp | `k8sPvc` → `disk` | `existence` | `optional` | 서버 자동 생성: CSI가 존 디스크를 합성한다 — 단 첫 소비자(Pod) 시점(기본 SC standard-rwo WaitForFirstConsumer 실측) | gcp에서 `k8sPvc`와 `disk`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 서버 자동 생성: CSI가 존 디스크를 합성한다 — 단 첫 소비자(Pod) 시점(기본 SC standard-rwo WaitForFirstConsumer 실측). | 5 |
| gcp | `k8sPvc` → `disk` | `lifecycle` | `holds` | 자동 정리 조건: PVC 삭제가 합성 디스크를 지운다(기본 reclaim Delete 실측) | gcp에서 `k8sPvc`와 `disk`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 자동 정리 조건: PVC 삭제가 합성 디스크를 지운다(기본 reclaim Delete 실측). | 1 |
| gcp | `k8sService` → `k8sCluster` | `function` | `holds` | 기능상 주의: Service 삭제를 컨트롤 플레인이 막지 않는다 — 기능 신호는 클러스터 안에서 서비스 이름으로의 HTTP 접속 | gcp에서 `k8sService`와 `k8sCluster`의 기능 의존성(대상 변화로 실제 기능이 깨지는지)을 확인한 결과는 `관계 확인됨`이다. 조건: 기능상 주의: Service 삭제를 컨트롤 플레인이 막지 않는다 — 기능 신호는 클러스터 안에서 서비스 이름으로의 HTTP 접속. 기능 확인 신호는 클러스터 안에서 서비스 이름으로 접속되는지이다. | 6 |
| gcp | `k8sService` → `loadBalancer` | `existence` | `optional` | 서버 자동 생성: type=LoadBalancer 서비스가 LB 성좌(forwardingRule+targetPool+방화벽)를 합성한다 | gcp에서 `k8sService`와 `loadBalancer`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 서버 자동 생성: type=LoadBalancer 서비스가 LB 성좌(forwardingRule+targetPool+방화벽)를 합성한다. | 2 |
| gcp | `k8sService` → `loadBalancer` | `lifecycle` | `holds` | 자동 정리 조건: Service 삭제가 성좌 전체(FR·targetPool·방화벽)를 함께 지운다 | gcp에서 `k8sService`와 `loadBalancer`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 자동 정리 조건: Service 삭제가 성좌 전체(FR·targetPool·방화벽)를 함께 지운다. | 2 |
| gcp | `loadBalancer` → `network` | `existence` | `optional` | 서버 기본값: EXTERNAL은 불참 · INTERNAL은 서브넷에서 역산 | gcp에서 `loadBalancer`와 `network`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 서버 기본값: EXTERNAL은 불참 · INTERNAL은 서브넷에서 역산. | 4 |
| gcp | `loadBalancer` → `subnet` | `existence` | `optional` | 스킴 조건: EXTERNAL 불참 · INTERNAL 필수 | gcp에서 `loadBalancer`와 `subnet`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 스킴 조건: EXTERNAL 불참 · INTERNAL 필수. | 4 |
| gcp | `network` → `internetGateway` | `function` | `holds` | 기능상 주의: 자동 생성 기본 라우트(0.0.0.0/0→default-internet-gateway) 삭제를 컨트롤 플레인이 막지 않는다 | gcp에서 `network`와 `internetGateway`의 기능 의존성(대상 변화로 실제 기능이 깨지는지)을 확인한 결과는 `관계 확인됨`이다. 조건: 기능상 주의: 자동 생성 기본 라우트(0.0.0.0/0→default-internet-gateway) 삭제를 컨트롤 플레인이 막지 않는다. 기능 확인 신호는 외부에서 TCP로 도달할 수 있는지이다. | 5 |
| gcp | `nic` → `network` | `existence` | `optional` | 별도 조건 없음 | gcp에서 `nic`와 `network`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 별도 조건 없음. | 2 |
| gcp | `nic` → `subnet` | `existence` | `optional` | 네트워크 모드 조건: custom에선 필수 · auto에선 서버가 리전 서브넷 대체 | gcp에서 `nic`와 `subnet`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 네트워크 모드 조건: custom에선 필수 · auto에선 서버가 리전 서브넷 대체. | 4 |
| gcp | `nic` → `subnet` | `lifecycle` | `holds` | 별도 조건 없음 | gcp에서 `nic`와 `subnet`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 별도 조건 없음. | 3 |
| gcp | `subnet` → `network` | `existence` | `required` | 별도 조건 없음 | gcp에서 `subnet`와 `network`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 별도 조건 없음. | 3 |
| gcp | `subnet` → `network` | `lifecycle` | `holds` | 별도 조건 없음 | gcp에서 `subnet`와 `network`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 별도 조건 없음. | 3 |
| gcp | `vm` → `customImage` | `existence` | `optional` | 별도 조건 없음 | gcp에서 `vm`와 `customImage`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 별도 조건 없음. | 2 |
| gcp | `vm` → `disk` | `existence` | `required` | 두 자원의 호환 조건: 디스크와 인스턴스의 존이 일치해야 한다 | gcp에서 `vm`와 `disk`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 두 자원의 호환 조건: 디스크와 인스턴스의 존이 일치해야 한다. | 4 |
| gcp | `vm` → `disk` | `lifecycle` | `holds` | 별도 조건 없음 | gcp에서 `vm`와 `disk`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 별도 조건 없음. | 3 |
| gcp | `vm` → `firewall` | `function` | `holds` | 기능상 주의: 네트워크 스코프 방화벽 규칙 삭제를 컨트롤 플레인이 막지 않는다 — gcp 방화벽은 자원 간 부착이 아니라 규칙이라 관계 변이가 없다(규칙 변이가 유일한 경로) | gcp에서 `vm`와 `firewall`의 기능 의존성(대상 변화로 실제 기능이 깨지는지)을 확인한 결과는 `관계 확인됨`이다. 조건: 기능상 주의: 네트워크 스코프 방화벽 규칙 삭제를 컨트롤 플레인이 막지 않는다 — gcp 방화벽은 자원 간 부착이 아니라 규칙이라 관계 변이가 없다(규칙 변이가 유일한 경로). 기능 확인 신호는 외부에서 TCP로 도달할 수 있는지이다. | 5 |
| gcp | `vm` → `iamRole` | `existence` | `optional` | 별도 조건 없음 | gcp에서 `vm`와 `iamRole`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 별도 조건 없음. | 3 |
| gcp | `vm` → `image` | `existence` | `optional` | 선택 규칙: 부트 디스크 원천 — initializeParams.sourceImage ∨ 기존 디스크(source) 중 정확히 하나(배타적) | gcp에서 `vm`와 `image`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `선택`이다. 조건: 선택 규칙: 부트 디스크 원천 — initializeParams.sourceImage ∨ 기존 디스크(source) 중 정확히 하나(배타적). | 6 |
| gcp | `vm` → `nic` | `existence` | `required` | 별도 조건 없음 | gcp에서 `vm`와 `nic`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 별도 조건 없음. | 2 |
| gcp | `vm` → `publicIp` | `function` | `holds` | 기능상 주의: RUNNING 인스턴스에서 accessConfig 삭제를 컨트롤 플레인이 막지 않는다 — 기능 신호는 외부 TCP 22 도달성 | gcp에서 `vm`와 `publicIp`의 기능 의존성(대상 변화로 실제 기능이 깨지는지)을 확인한 결과는 `관계 확인됨`이다. 조건: 기능상 주의: RUNNING 인스턴스에서 accessConfig 삭제를 컨트롤 플레인이 막지 않는다 — 기능 신호는 외부 TCP 22 도달성. 기능 확인 신호는 외부에서 TCP로 도달할 수 있는지이다. | 6 |
| gcp | `vpn` → `network` | `existence` | `required` | 별도 조건 없음 | gcp에서 `vpn`와 `network`의 생성 의존성(대상이 없거나 잘못됐을 때 주체를 만들 수 있는지)을 확인한 결과는 `필수`이다. 조건: 별도 조건 없음. | 2 |
| gcp | `vpn` → `network` | `lifecycle` | `holds` | 별도 조건 없음 | gcp에서 `vpn`와 `network`의 수명주기 의존성(대상이 연결된 상태에서 삭제·정리가 가능한지)을 확인한 결과는 `관계 확인됨`이다. 조건: 별도 조건 없음. | 2 |

## 6. 모든 실험 evidence 참조

아래 표는 claims에 실제로 사용된 실험 참조를 중복 없이 모은 것이다. `정의 위치`는 단계 이름과 요청을 만든 코드다. `결과 위치`에는 CSP 응답의 오류 코드와 발췌가 있다. 따라서 표의 어느 행도 단순한 내부 약어로 끝나지 않고, 항상 재현 원천으로 내려갈 수 있다.

| 실험 | 단계 ID | 단계 ID를 읽는 법 | 관측 | 정의 위치 | 결과 위치 |
|---|---|---|---|---|---|
| `aws-apply-2026-07-30` | `A.dangling-vm-keyname` | `A`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `dangling-vm-keyname`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `InvalidKeyPair.NotFound` 오류를 돌려줬다 | `experiments/aws-apply-2026-07-30/run.py:90`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-apply-2026-07-30/results.json` |
| `aws-apply-2026-07-30` | `A.dangling-vm-subnet` | `A`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `dangling-vm-subnet`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `InvalidSubnetID.NotFound` 오류를 돌려줬다 | `experiments/aws-apply-2026-07-30/run.py:88`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-apply-2026-07-30/results.json` |
| `aws-apply-2026-07-30` | `A.dryrun-vm-default-vpc` | `A`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `dryrun-vm-default-vpc`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `DryRunOperation` 오류를 돌려줬다 | `experiments/aws-apply-2026-07-30/run.py:87`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-apply-2026-07-30/results.json` |
| `aws-apply-2026-07-30` | `A.omit-nic-subnet` | `A`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `omit-nic-subnet`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `--subnet-id` 오류를 돌려줬다 | `experiments/aws-apply-2026-07-30/run.py:79`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-apply-2026-07-30/results.json` |
| `aws-apply-2026-07-30` | `C.delete-sg-attached` | `C`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-sg-attached`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `DependencyViolation` 오류를 돌려줬다 | `experiments/aws-apply-2026-07-30/run.py:118`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-apply-2026-07-30/results.json` |
| `aws-apply-2026-07-30` | `C.delete-subnet-in-use` | `C`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-subnet-in-use`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `DependencyViolation` 오류를 돌려줬다 | `experiments/aws-apply-2026-07-30/run.py:114`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-apply-2026-07-30/results.json` |
| `aws-apply-2026-07-30` | `C.delete-vpc-in-use` | `C`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-vpc-in-use`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `DependencyViolation` 오류를 돌려줬다 | `experiments/aws-apply-2026-07-30/run.py:116`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-apply-2026-07-30/results.json` |
| `aws-apply-2026-07-30` | `D.delete-sg` | `D`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-sg`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-apply-2026-07-30/run.py:124`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-apply-2026-07-30/results.json` |
| `aws-apply-2026-07-30` | `D.delete-subnet` | `D`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-subnet`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-apply-2026-07-30/run.py:126`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-apply-2026-07-30/results.json` |
| `aws-apply-2026-07-30` | `D.delete-vpc` | `D`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-vpc`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-apply-2026-07-30/run.py:128`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-apply-2026-07-30/results.json` |
| `aws-apply2-2026-07-31` | `B1.dangling-subnet-vpc` | `B1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `dangling-subnet-vpc`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `InvalidVpcID.NotFound` 오류를 돌려줬다 | `experiments/aws-apply2-2026-07-31/run.py:64`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-apply2-2026-07-31/results.json` |
| `aws-apply2-2026-07-31` | `B2.server-filled-vpc` | `B2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `server-filled-vpc`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-apply2-2026-07-31/run.py:76`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-apply2-2026-07-31/results.json` |
| `aws-apply2-2026-07-31` | `B2.sg-omit-vpc` | `B2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `sg-omit-vpc`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-apply2-2026-07-31/run.py:71`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-apply2-2026-07-31/results.json` |
| `aws-apply2-2026-07-31` | `B3.eni-omit-groups` | `B3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `eni-omit-groups`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-apply2-2026-07-31/run.py:95`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-apply2-2026-07-31/results.json` |
| `aws-apply2-2026-07-31` | `B3.server-filled-groups` | `B3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `server-filled-groups`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-apply2-2026-07-31/run.py:100`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-apply2-2026-07-31/results.json` |
| `aws-apply2-2026-07-31` | `B4.alb-one-subnet` | `B4`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `alb-one-subnet`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `ValidationError` 오류를 돌려줬다 | `experiments/aws-apply2-2026-07-31/run.py:109`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-apply2-2026-07-31/results.json` |
| `aws-apply2-2026-07-31` | `B4.lb-omit-subnets` | `B4`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `lb-omit-subnets`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `ValidationError` 오류를 돌려줬다 | `experiments/aws-apply2-2026-07-31/run.py:106`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-apply2-2026-07-31/results.json` |
| `aws-apply2-2026-07-31` | `F.internal-nlb-one-subnet-no-sg` | `F`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `internal-nlb-one-subnet-no-sg`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-apply2-2026-07-31/run_fix.py:39`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-apply2-2026-07-31/results.json` |
| `aws-apply2-2026-07-31` | `F2.internal-nlb-omit-subnets` | `F2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `internal-nlb-omit-subnets`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `ValidationError` 오류를 돌려줬다 | `experiments/aws-apply2-2026-07-31/run_fix2.py:22`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-apply2-2026-07-31/results.json` |
| `aws-cimg-2026-07-31` | `A1.dangling-source-instance` | `A1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `dangling-source-instance`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `InvalidParameterValue` 오류를 돌려줬다 | `experiments/aws-cimg-2026-07-31/run.py:70`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-cimg-2026-07-31/results.json` |
| `aws-cimg-2026-07-31` | `A3.ami-available` | `A3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `ami-available`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-cimg-2026-07-31/run.py:89`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-cimg-2026-07-31/results.json` |
| `aws-cimg-2026-07-31` | `A4.ami-snapshots` | `A4`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `ami-snapshots`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-cimg-2026-07-31/run.py:91`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-cimg-2026-07-31/results.json` |
| `aws-cimg-2026-07-31` | `C1.run-instance-from-custom-ami` | `C1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `run-instance-from-custom-ami`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-cimg-2026-07-31/run.py:107`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-cimg-2026-07-31/results.json` |
| `aws-cimg-2026-07-31` | `C2.vm-running` | `C2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `vm-running`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-cimg-2026-07-31/run.py:113`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-cimg-2026-07-31/results.json` |
| `aws-dns-2026-07-31` | `A0.private-zone-without-vpc` | `A0`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `private-zone-without-vpc`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `InvalidInput` 오류를 돌려줬다 | `experiments/aws-dns-2026-07-31/run.py:46`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-dns-2026-07-31/results.json` |
| `aws-dns-2026-07-31` | `A1.record-without-zone` | `A1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `record-without-zone`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `NoSuchHostedZone` 오류를 돌려줬다 | `experiments/aws-dns-2026-07-31/run.py:51`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-dns-2026-07-31/results.json` |
| `aws-dns-2026-07-31` | `A2.create-private-zone` | `A2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `create-private-zone`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-dns-2026-07-31/run.py:61`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-dns-2026-07-31/results.json` |
| `aws-dns-2026-07-31` | `A3.create-record` | `A3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `create-record`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-dns-2026-07-31/run.py:70`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-dns-2026-07-31/results.json` |
| `aws-dns-2026-07-31` | `L1.delete-zone-with-record` | `L1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-zone-with-record`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `HostedZoneNotEmpty` 오류를 돌려줬다 | `experiments/aws-dns-2026-07-31/run.py:74`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-dns-2026-07-31/results.json` |
| `aws-dns-2026-07-31` | `L3.delete-zone-after` | `L3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-zone-after`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-dns-2026-07-31/run.py:80`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-dns-2026-07-31/results.json` |
| `aws-eks-2026-07-31` | `E1.omit-vpc-config` | `E1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `omit-vpc-config`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `--resources-vpc-config` 오류를 돌려줬다 | `experiments/aws-eks-2026-07-31/run.py:34`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-eks-2026-07-31/results.json` |
| `aws-eks-2026-07-31` | `E2.omit-role` | `E2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `omit-role`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `--role-arn` 오류를 돌려줬다 | `experiments/aws-eks-2026-07-31/run.py:37`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-eks-2026-07-31/results.json` |
| `aws-eks-2026-07-31` | `E3.one-subnet` | `E3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `one-subnet`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `InvalidParameterException` 오류를 돌려줬다 | `experiments/aws-eks-2026-07-31/run.py:40`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-eks-2026-07-31/results.json` |
| `aws-eks2-2026-07-31` | `C1.one-real-subnet` | `C1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `one-real-subnet`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `InvalidParameterException` 오류를 돌려줬다 | `experiments/aws-eks2-2026-07-31/run.py:56`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-eks2-2026-07-31/results.json` |
| `aws-eks2-2026-07-31` | `C2.two-subnets-same-az` | `C2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `two-subnets-same-az`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `InvalidParameterException` 오류를 돌려줬다 | `experiments/aws-eks2-2026-07-31/run.py:60`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-eks2-2026-07-31/results.json` |
| `aws-eks3-2026-07-31` | `F2.delete-subnet1` | `F2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-subnet1`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-eks3-2026-07-31/run.py:138`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-eks3-2026-07-31/results.json` |
| `aws-eks3-2026-07-31` | `F3.delete-vpc` | `F3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-vpc`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-eks3-2026-07-31/run.py:140`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-eks3-2026-07-31/results.json` |
| `aws-eks3-2026-07-31` | `K1.create-cluster` | `K1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `create-cluster`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-eks3-2026-07-31/run.py:82`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-eks3-2026-07-31/results.json` |
| `aws-eks3-2026-07-31` | `K2.cluster-active` | `K2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `cluster-active`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-eks3-2026-07-31/run.py:100`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-eks3-2026-07-31/results.json` |
| `aws-eks3-2026-07-31` | `K3.cluster-shape` | `K3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `cluster-shape`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-eks3-2026-07-31/run.py:104`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-eks3-2026-07-31/results.json` |
| `aws-eks3-2026-07-31` | `L1.delete-subnet-in-use` | `L1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-subnet-in-use`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `DependencyViolation` 오류를 돌려줬다 | `experiments/aws-eks3-2026-07-31/run.py:113`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-eks3-2026-07-31/results.json` |
| `aws-eks3-2026-07-31` | `L2.delete-vpc-in-use` | `L2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-vpc-in-use`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `DependencyViolation` 오류를 돌려줬다 | `experiments/aws-eks3-2026-07-31/run.py:115`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-eks3-2026-07-31/results.json` |
| `aws-eks3-2026-07-31` | `R.create-role` | `R`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `create-role`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-eks3-2026-07-31/run.py:58`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-eks3-2026-07-31/results.json` |
| `aws-ekspvc-2026-07-31` | `I0.csi-controller-error` | `I0`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `csi-controller-error`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `FailedPrecondition` 오류를 돌려줬다 | `experiments/aws-ekspvc-2026-07-31/results.json:346`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/aws-ekspvc-2026-07-31/results.json` |
| `aws-ekspvc-2026-07-31` | `J1.annotate-sa-with-irsa` | `J1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `annotate-sa-with-irsa`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-ekspvc-2026-07-31/results.json:391`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/aws-ekspvc-2026-07-31/results.json` |
| `aws-ekspvc-2026-07-31` | `J10.delete-pvc` | `J10`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-pvc`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-ekspvc-2026-07-31/results.json:437`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/aws-ekspvc-2026-07-31/results.json` |
| `aws-ekspvc-2026-07-31` | `J11.volumes-after-delete` | `J11`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `volumes-after-delete`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-ekspvc-2026-07-31/results.json:442`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/aws-ekspvc-2026-07-31/results.json` |
| `aws-ekspvc-2026-07-31` | `J6.pvc-bound` | `J6`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `pvc-bound`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-ekspvc-2026-07-31/results.json:416`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/aws-ekspvc-2026-07-31/results.json` |
| `aws-ekspvc-2026-07-31` | `J7.volumes-after-pod` | `J7`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `volumes-after-pod`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-ekspvc-2026-07-31/results.json:421`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/aws-ekspvc-2026-07-31/results.json` |
| `aws-ekspvc-2026-07-31` | `J8.pv-volumehandle` | `J8`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `pv-volumehandle`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-ekspvc-2026-07-31/results.json:427`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/aws-ekspvc-2026-07-31/results.json` |
| `aws-ekspvc-2026-07-31` | `N2.nodegroup-active` | `N2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `nodegroup-active`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-ekspvc-2026-07-31/run.py:180`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-ekspvc-2026-07-31/results.json` |
| `aws-fs-2026-07-31` | `A1.create-filesystem-no-network` | `A1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `create-filesystem-no-network`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-fs-2026-07-31/run.py:45`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-fs-2026-07-31/results.json` |
| `aws-fs-2026-07-31` | `A3.mount-target-omit-subnet` | `A3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `mount-target-omit-subnet`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `--subnet-id` 오류를 돌려줬다 | `experiments/aws-fs-2026-07-31/run.py:75`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-fs-2026-07-31/results.json` |
| `aws-fs-2026-07-31` | `A4.mount-target-dangling-subnet` | `A4`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `mount-target-dangling-subnet`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `SubnetNotFound` 오류를 돌려줬다 | `experiments/aws-fs-2026-07-31/run.py:79`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-fs-2026-07-31/results.json` |
| `aws-fs-2026-07-31` | `A6.mount-target-available` | `A6`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `mount-target-available`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-fs-2026-07-31/run.py:99`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-fs-2026-07-31/results.json` |
| `aws-fs-2026-07-31` | `L1.delete-subnet-in-use` | `L1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-subnet-in-use`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `DependencyViolation` 오류를 돌려줬다 | `experiments/aws-fs-2026-07-31/run.py:102`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-fs-2026-07-31/results.json` |
| `aws-fs-2026-07-31` | `T4.delete-subnet` | `T4`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-subnet`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-fs-2026-07-31/run.py:123`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-fs-2026-07-31/results.json` |
| `aws-func-2026-07-31` | `F1.reachable-baseline` | `F1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `reachable-baseline`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-func-2026-07-31/run.py:109`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-func-2026-07-31/results.json` |
| `aws-func-2026-07-31` | `F2.unreachable-after-detach` | `F2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `unreachable-after-detach`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-func-2026-07-31/run.py:117`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-func-2026-07-31/results.json` |
| `aws-func-2026-07-31` | `F3.reachable-again` | `F3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `reachable-again`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-func-2026-07-31/run.py:124`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-func-2026-07-31/results.json` |
| `aws-func-2026-07-31` | `M1.disassociate-while-running` | `M1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `disassociate-while-running`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-func-2026-07-31/run.py:111`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-func-2026-07-31/results.json` |
| `aws-func-2026-07-31` | `M1b.instance-public-ip-now` | `M1b`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `instance-public-ip-now`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-func-2026-07-31/run.py:113`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-func-2026-07-31/results.json` |
| `aws-func-2026-07-31` | `M2.reassociate-eip` | `M2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `reassociate-eip`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-func-2026-07-31/run.py:119`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-func-2026-07-31/results.json` |
| `aws-func2-2026-07-31` | `F1.reachable-baseline` | `F1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `reachable-baseline`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-func2-2026-07-31/run.py:138`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-func2-2026-07-31/results.json` |
| `aws-func2-2026-07-31` | `F2.unreachable-empty-sg` | `F2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `unreachable-empty-sg`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-func2-2026-07-31/run.py:143`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-func2-2026-07-31/results.json` |
| `aws-func2-2026-07-31` | `F3.reachable-again` | `F3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `reachable-again`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-func2-2026-07-31/run.py:147`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-func2-2026-07-31/results.json` |
| `aws-func2-2026-07-31` | `F4.unreachable-no-route` | `F4`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `unreachable-no-route`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-func2-2026-07-31/run.py:152`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-func2-2026-07-31/results.json` |
| `aws-func2-2026-07-31` | `F5.reachable-final` | `F5`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `reachable-final`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-func2-2026-07-31/run.py:157`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-func2-2026-07-31/results.json` |
| `aws-func2-2026-07-31` | `M1.swap-to-empty-sg` | `M1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `swap-to-empty-sg`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-func2-2026-07-31/run.py:140`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-func2-2026-07-31/results.json` |
| `aws-func2-2026-07-31` | `M2.restore-sg22` | `M2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `restore-sg22`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-func2-2026-07-31/run.py:144`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-func2-2026-07-31/results.json` |
| `aws-func2-2026-07-31` | `M3.delete-default-route` | `M3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-default-route`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-func2-2026-07-31/run.py:149`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-func2-2026-07-31/results.json` |
| `aws-func2-2026-07-31` | `M4.recreate-route` | `M4`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `recreate-route`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-func2-2026-07-31/run.py:153`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-func2-2026-07-31/results.json` |
| `aws-func2-2026-07-31` | `R10.run-instance` | `R10`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `run-instance`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-func2-2026-07-31/run.py:109`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-func2-2026-07-31/results.json` |
| `aws-iamfunc2-2026-08-02` | `F0b.control-nodegroup-terminal` | `F0b`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `control-nodegroup-terminal`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-iamfunc2-2026-08-02/run.py:234`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-iamfunc2-2026-08-02/results.json` |
| `aws-iamfunc2-2026-08-02` | `F1b.nodegroup-terminal-under-mutation` | `F1b`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `nodegroup-terminal-under-mutation`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `CREATE_FAILED` 오류를 돌려줬다 | `experiments/aws-iamfunc2-2026-08-02/run.py:249`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-iamfunc2-2026-08-02/results.json` |
| `aws-iamfunc2-2026-08-02` | `F1c.policy-still-detached` | `F1c`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `policy-still-detached`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-iamfunc2-2026-08-02/run.py:251`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-iamfunc2-2026-08-02/results.json` |
| `aws-iamfunc2-2026-08-02` | `F2b.nodegroup-terminal-after-restore` | `F2b`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `nodegroup-terminal-after-restore`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-iamfunc2-2026-08-02/run.py:262`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-iamfunc2-2026-08-02/results.json` |
| `aws-iamfunc2-2026-08-02` | `M1.detach-policy-while-active` | `M1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `detach-policy-while-active`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-iamfunc2-2026-08-02/run.py:239`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-iamfunc2-2026-08-02/results.json` |
| `aws-iamfunc2-2026-08-02` | `M2.reattach-policy` | `M2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `reattach-policy`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-iamfunc2-2026-08-02/run.py:257`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-iamfunc2-2026-08-02/results.json` |
| `aws-image-2026-07-31` | `D1.omit-image-and-lt` | `D1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `omit-image-and-lt`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `MissingParameter` 오류를 돌려줬다 | `experiments/aws-image-2026-07-31/run.py:44`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-image-2026-07-31/results.json` |
| `aws-image-2026-07-31` | `D2b.dangling-wellformed-ami` | `D2b`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `dangling-wellformed-ami`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `InvalidAMIID.Malformed` 오류를 돌려줬다 | `experiments/aws-image-2026-07-31/results.json:38`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/aws-image-2026-07-31/results.json` |
| `aws-image-2026-07-31` | `D3b.valid-ami-dryrun` | `D3b`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `valid-ami-dryrun`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `DryRunOperation` 오류를 돌려줬다 | `experiments/aws-image-2026-07-31/run.py:57`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-image-2026-07-31/results.json` |
| `aws-k8s-synth-2026-07-31` | `L2.elb-after-delete` | `L2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `elb-after-delete`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-k8s-synth-2026-07-31/run.py:209`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-k8s-synth-2026-07-31/results.json` |
| `aws-k8s-synth-2026-07-31` | `S2.svc-ingress-hint` | `S2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `svc-ingress-hint`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-k8s-synth-2026-07-31/run.py:184`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-k8s-synth-2026-07-31/results.json` |
| `aws-k8s-synth-2026-07-31` | `S3.elb-after-svc` | `S3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `elb-after-svc`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-k8s-synth-2026-07-31/run.py:186`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-k8s-synth-2026-07-31/results.json` |
| `aws-k8s-synth-2026-07-31` | `S5.sgs-after-svc` | `S5`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `sgs-after-svc`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-k8s-synth-2026-07-31/run.py:191`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-k8s-synth-2026-07-31/results.json` |
| `aws-k8s-synth2-2026-07-31` | `I3.ingress-address-hint` | `I3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `ingress-address-hint`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-k8s-synth2-2026-07-31/run.py:147`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-k8s-synth2-2026-07-31/results.json` |
| `aws-k8s-synth2-2026-07-31` | `I4.elb-after-ingress` | `I4`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `elb-after-ingress`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-k8s-synth2-2026-07-31/run.py:150`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-k8s-synth2-2026-07-31/results.json` |
| `aws-k8s-synth2-2026-07-31` | `K4.ingressclasses` | `K4`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `ingressclasses`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-k8s-synth2-2026-07-31/run.py:138`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-k8s-synth2-2026-07-31/results.json` |
| `aws-sig4-2026-07-31` | `F1.imds-credentials-work` | `F1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `imds-credentials-work`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-sig4-2026-07-31/run.py:188`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-sig4-2026-07-31/results.json` |
| `aws-sig4-2026-07-31` | `F2.imds-credentials-lost` | `F2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `imds-credentials-lost`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-sig4-2026-07-31/run.py:205`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-sig4-2026-07-31/results.json` |
| `aws-sig4-2026-07-31` | `F3.imds-credentials-again` | `F3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `imds-credentials-again`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-sig4-2026-07-31/run.py:211`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-sig4-2026-07-31/results.json` |
| `aws-sig4-2026-07-31` | `G1.egress-works` | `G1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `egress-works`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-sig4-2026-07-31/run.py:216`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-sig4-2026-07-31/results.json` |
| `aws-sig4-2026-07-31` | `G2.egress-lost` | `G2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `egress-lost`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-sig4-2026-07-31/run.py:221`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-sig4-2026-07-31/results.json` |
| `aws-sig4-2026-07-31` | `G3.egress-again` | `G3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `egress-again`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-sig4-2026-07-31/run.py:227`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-sig4-2026-07-31/results.json` |
| `aws-sig4-2026-07-31` | `M1.disassociate-profile` | `M1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `disassociate-profile`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-sig4-2026-07-31/run.py:197`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-sig4-2026-07-31/results.json` |
| `aws-sig4-2026-07-31` | `M2.reassociate-profile` | `M2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `reassociate-profile`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-sig4-2026-07-31/run.py:207`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-sig4-2026-07-31/results.json` |
| `aws-sig4-2026-07-31` | `M3.delete-default-route` | `M3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-default-route`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-sig4-2026-07-31/run.py:218`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-sig4-2026-07-31/results.json` |
| `aws-sig4-2026-07-31` | `M4.recreate-route` | `M4`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `recreate-route`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-sig4-2026-07-31/run.py:223`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-sig4-2026-07-31/results.json` |
| `aws-vpn-2026-07-31` | `A2.attach-to-dangling-vpc` | `A2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `attach-to-dangling-vpc`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `InvalidVpcID.NotFound` 오류를 돌려줬다 | `experiments/aws-vpn-2026-07-31/run.py:47`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-vpn-2026-07-31/results.json` |
| `aws-vpn-2026-07-31` | `A3.attach-to-vpc` | `A3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `attach-to-vpc`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-vpn-2026-07-31/run.py:56`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-vpn-2026-07-31/results.json` |
| `aws-vpn-2026-07-31` | `A4.attached` | `A4`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `attached`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-vpn-2026-07-31/run.py:70`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-vpn-2026-07-31/results.json` |
| `aws-vpn-2026-07-31` | `L1.delete-vpc-with-vgw` | `L1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-vpc-with-vgw`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `DependencyViolation` 오류를 돌려줬다 | `experiments/aws-vpn-2026-07-31/run.py:73`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-vpn-2026-07-31/results.json` |
| `aws-vpn-2026-07-31` | `L2.delete-vpc-after-detach` | `L2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-vpc-after-detach`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/aws-vpn-2026-07-31/run.py:91`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/aws-vpn-2026-07-31/results.json` |
| `azure-aks2-2026-07-31` | `A1.create-no-subnet-nowait` | `A1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `create-no-subnet-nowait`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-aks2-2026-07-31/run.py:64`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-aks2-2026-07-31/results.json` |
| `azure-aks2-2026-07-31` | `A2.provisioning-final` | `A2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `provisioning-final`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-aks2-2026-07-31/run.py:81`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-aks2-2026-07-31/results.json` |
| `azure-aks2-2026-07-31` | `A4.synthesized-vnets-in-node-rg` | `A4`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `synthesized-vnets-in-node-rg`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-aks2-2026-07-31/run.py:92`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-aks2-2026-07-31/results.json` |
| `azure-aks2-2026-07-31` | `B1.nodepool-add` | `B1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `nodepool-add`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-aks2-2026-07-31/run.py:96`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-aks2-2026-07-31/results.json` |
| `azure-aks2-2026-07-31` | `B2.nodepool-delete` | `B2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `nodepool-delete`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-aks2-2026-07-31/run.py:100`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-aks2-2026-07-31/results.json` |
| `azure-aks3-2026-07-31` | `F2.delete-subnet` | `F2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-subnet`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-aks3-2026-07-31/run.py:113`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-aks3-2026-07-31/results.json` |
| `azure-aks3-2026-07-31` | `F3.delete-vnet` | `F3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-vnet`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-aks3-2026-07-31/run.py:116`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-aks3-2026-07-31/results.json` |
| `azure-aks3-2026-07-31` | `L1.delete-subnet-in-use` | `L1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-subnet-in-use`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `InUseSubnetCannotBeDeleted` 오류를 돌려줬다 | `experiments/azure-aks3-2026-07-31/run.py:91`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-aks3-2026-07-31/results.json` |
| `azure-aks3-2026-07-31` | `L2.delete-vnet-in-use` | `L2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-vnet-in-use`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `InUseSubnetCannotBeDeleted` 오류를 돌려줬다 | `experiments/azure-aks3-2026-07-31/run.py:94`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-aks3-2026-07-31/results.json` |
| `azure-apply-2026-07-30` | `A.apply.dangling-lb-pip` | `A`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `apply.dangling-lb-pip`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `InvalidResourceReference` 오류를 돌려줬다 | `experiments/azure-apply-2026-07-30/results.json:38`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/azure-apply-2026-07-30/results.json` |
| `azure-apply-2026-07-30` | `A.apply.dangling-nic-subnet` | `A`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `apply.dangling-nic-subnet`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `InvalidResourceReference` 오류를 돌려줬다 | `experiments/azure-apply-2026-07-30/results.json:14`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/azure-apply-2026-07-30/results.json` |
| `azure-apply-2026-07-30` | `A.apply.dangling-subnet-parent` | `A`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `apply.dangling-subnet-parent`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `ResourceNotFound` 오류를 돌려줬다 | `experiments/azure-apply-2026-07-30/results.json:22`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/azure-apply-2026-07-30/results.json` |
| `azure-apply-2026-07-30` | `A.apply.dangling-vm-nic` | `A`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `apply.dangling-vm-nic`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `NotFound` 오류를 돌려줬다 | `experiments/azure-apply-2026-07-30/results.json:30`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/azure-apply-2026-07-30/results.json` |
| `azure-apply-2026-07-30` | `A.apply.omit-vm-nic` | `A`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `apply.omit-vm-nic`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `InvalidParameter` 오류를 돌려줬다 | `experiments/azure-apply-2026-07-30/results.json:6`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/azure-apply-2026-07-30/results.json` |
| `azure-apply-2026-07-30` | `B.build-chain` | `B`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `build-chain`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-apply-2026-07-30/run.py:60`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-apply-2026-07-30/results.json` |
| `azure-apply-2026-07-30` | `C.delete-nsg-attached` | `C`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-nsg-attached`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `InUseNetworkSecurityGroupCannotBeDeleted` 오류를 돌려줬다 | `experiments/azure-apply-2026-07-30/run.py:68`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-apply-2026-07-30/results.json` |
| `azure-apply-2026-07-30` | `C.delete-subnet-in-use` | `C`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-subnet-in-use`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `InUseSubnetCannotBeDeleted` 오류를 돌려줬다 | `experiments/azure-apply-2026-07-30/run.py:63`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-apply-2026-07-30/results.json` |
| `azure-apply-2026-07-30` | `C.delete-vnet-in-use` | `C`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-vnet-in-use`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `InUseSubnetCannotBeDeleted` 오류를 돌려줬다 | `experiments/azure-apply-2026-07-30/run.py:66`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-apply-2026-07-30/results.json` |
| `azure-apply-2026-07-30` | `D.delete-nsg` | `D`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-nsg`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-apply-2026-07-30/run.py:75`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-apply-2026-07-30/results.json` |
| `azure-apply-2026-07-30` | `D.delete-subnet` | `D`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-subnet`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-apply-2026-07-30/run.py:76`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-apply-2026-07-30/results.json` |
| `azure-apply-2026-07-30` | `D.delete-vnet` | `D`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-vnet`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-apply-2026-07-30/run.py:78`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-apply-2026-07-30/results.json` |
| `azure-apply2-2026-07-30` | `C.delete-pip-attached` | `C`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-pip-attached`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `PublicIPAddressCannotBeDeleted` 오류를 돌려줬다 | `experiments/azure-apply2-2026-07-30/run.py:59`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-apply2-2026-07-30/results.json` |
| `azure-apply2-2026-07-30` | `D.delete-pip1` | `D`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-pip1`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-apply2-2026-07-30/run.py:66`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-apply2-2026-07-30/results.json` |
| `azure-apply2-2026-07-30` | `E0.build-chain2` | `E0`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `build-chain2`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-apply2-2026-07-30/run.py:55`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-apply2-2026-07-30/results.json` |
| `azure-apply2-2026-07-30` | `E1.apply-vnet-without-subnet` | `E1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `apply-vnet-without-subnet`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-apply2-2026-07-30/run.py:49`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-apply2-2026-07-30/results.json` |
| `azure-apply3-2026-07-30` | `C1.delete-disk-attached` | `C1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-disk-attached`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `OperationNotAllowed` 오류를 돌려줬다 | `experiments/azure-apply3-2026-07-30/run.py:58`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-apply3-2026-07-30/results.json` |
| `azure-apply3-2026-07-30` | `D.delete-data-disk` | `D`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-data-disk`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-apply3-2026-07-30/run.py:67`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-apply3-2026-07-30/results.json` |
| `azure-apply3-2026-07-30` | `F0.build-vm-no-datadisk` | `F0`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `build-vm-no-datadisk`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-apply3-2026-07-30/run.py:49`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-apply3-2026-07-30/results.json` |
| `azure-cimg-2026-07-31` | `A1.dangling-source-disk` | `A1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `dangling-source-disk`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `NotFound` 오류를 돌려줬다 | `experiments/azure-cimg-2026-07-31/run.py:86`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-cimg-2026-07-31/results.json` |
| `azure-cimg-2026-07-31` | `A2.create-image-from-disk` | `A2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `create-image-from-disk`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-cimg-2026-07-31/run.py:92`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-cimg-2026-07-31/results.json` |
| `azure-cimg-2026-07-31` | `C1.create-vm-from-custom-image` | `C1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `create-vm-from-custom-image`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-cimg-2026-07-31/run.py:103`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-cimg-2026-07-31/results.json` |
| `azure-csi2-2026-07-31` | `K0.storage-rp-state` | `K0`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `storage-rp-state`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-csi2-2026-07-31/run.py:72`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-csi2-2026-07-31/results.json` |
| `azure-csi2-2026-07-31` | `K4.aks-identity-shape` | `K4`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `aks-identity-shape`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-csi2-2026-07-31/run.py:101`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-csi2-2026-07-31/results.json` |
| `azure-csi2-2026-07-31` | `P2.rwx-bound` | `P2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `rwx-bound`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-csi2-2026-07-31/run.py:122`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-csi2-2026-07-31/results.json` |
| `azure-csi2-2026-07-31` | `P4.storage-accounts-after` | `P4`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `storage-accounts-after`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-csi2-2026-07-31/run.py:129`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-csi2-2026-07-31/results.json` |
| `azure-csi2-2026-07-31` | `P5.pv-volumehandle-hint` | `P5`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `pv-volumehandle-hint`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-csi2-2026-07-31/run.py:132`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-csi2-2026-07-31/results.json` |
| `azure-csi2-2026-07-31` | `P6.delete-pvc` | `P6`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-pvc`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-csi2-2026-07-31/run.py:134`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-csi2-2026-07-31/results.json` |
| `azure-csi2-2026-07-31` | `P7.storage-accounts-after-delete` | `P7`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `storage-accounts-after-delete`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-csi2-2026-07-31/run.py:137`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-csi2-2026-07-31/results.json` |
| `azure-disj2-2026-08-01` | `E1.arm-both-subnet-and-pip` | `E1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `arm-both-subnet-and-pip`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `FrontendIPConfigHasBothSubnetAndPublicIP` 오류를 돌려줬다 | `experiments/azure-disj2-2026-08-01/run.py:104`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-disj2-2026-08-01/results.json` |
| `azure-disj2-2026-08-01` | `E2.arm-subnet-only` | `E2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `arm-subnet-only`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-disj2-2026-08-01/run.py:106`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-disj2-2026-08-01/results.json` |
| `azure-disj2-2026-08-01` | `E3.arm-pip-only` | `E3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `arm-pip-only`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-disj2-2026-08-01/run.py:107`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-disj2-2026-08-01/results.json` |
| `azure-dns-fs-2026-07-31` | `D1.record-without-zone` | `D1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `record-without-zone`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `ParentResourceNotFound` 오류를 돌려줬다 | `experiments/azure-dns-fs-2026-07-31/run.py:52`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-dns-fs-2026-07-31/results.json` |
| `azure-dns-fs-2026-07-31` | `D3.create-record` | `D3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `create-record`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-dns-fs-2026-07-31/run.py:58`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-dns-fs-2026-07-31/results.json` |
| `azure-dns-fs-2026-07-31` | `F1.create-storage-account` | `F1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `create-storage-account`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-dns-fs-2026-07-31/run.py:75`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-dns-fs-2026-07-31/results.json` |
| `azure-dns-fs-2026-07-31` | `F2.create-file-share` | `F2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `create-file-share`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-dns-fs-2026-07-31/run.py:79`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-dns-fs-2026-07-31/results.json` |
| `azure-func-2026-07-31` | `F1.reachable-baseline` | `F1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `reachable-baseline`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-func-2026-07-31/run.py:92`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-func-2026-07-31/results.json` |
| `azure-func-2026-07-31` | `F2.unreachable-after-detach` | `F2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `unreachable-after-detach`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-func-2026-07-31/run.py:101`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-func-2026-07-31/results.json` |
| `azure-func-2026-07-31` | `F3.reachable-again` | `F3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `reachable-again`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-func-2026-07-31/run.py:107`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-func-2026-07-31/results.json` |
| `azure-func-2026-07-31` | `M1.detach-pip-while-running` | `M1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `detach-pip-while-running`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-func-2026-07-31/run.py:94`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-func-2026-07-31/results.json` |
| `azure-func-2026-07-31` | `M1b.vm-still-running` | `M1b`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `vm-still-running`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-func-2026-07-31/run.py:98`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-func-2026-07-31/results.json` |
| `azure-func-2026-07-31` | `M2.reattach-pip` | `M2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `reattach-pip`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-func-2026-07-31/run.py:103`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-func-2026-07-31/results.json` |
| `azure-func-2026-07-31` | `R4.create-vm` | `R4`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `create-vm`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-func-2026-07-31/run.py:80`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-func-2026-07-31/results.json` |
| `azure-func2-2026-07-31` | `F1.reachable-baseline` | `F1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `reachable-baseline`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-func2-2026-07-31/run.py:103`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-func2-2026-07-31/results.json` |
| `azure-func2-2026-07-31` | `F2.unreachable-after-detach` | `F2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `unreachable-after-detach`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-func2-2026-07-31/run.py:112`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-func2-2026-07-31/results.json` |
| `azure-func2-2026-07-31` | `F3.reachable-again` | `F3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `reachable-again`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-func2-2026-07-31/run.py:117`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-func2-2026-07-31/results.json` |
| `azure-func2-2026-07-31` | `M1.detach-nsg-from-subnet` | `M1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `detach-nsg-from-subnet`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-func2-2026-07-31/run.py:108`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-func2-2026-07-31/results.json` |
| `azure-func2-2026-07-31` | `M2.reattach-nsg` | `M2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `reattach-nsg`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-func2-2026-07-31/run.py:113`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-func2-2026-07-31/results.json` |
| `azure-image-2026-07-31` | `A2.dangling-image` | `A2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `dangling-image`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `NotFound` 오류를 돌려줬다 | `experiments/azure-image-2026-07-31/run.py:71`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-image-2026-07-31/results.json` |
| `azure-image-2026-07-31` | `A3.omit-storageprofile` | `A3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `omit-storageprofile`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `InvalidParameter` 오류를 돌려줬다 | `experiments/azure-image-2026-07-31/results.json:40`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/azure-image-2026-07-31/results.json` |
| `azure-image-2026-07-31` | `B0.create-vm-from-image` | `B0`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `create-vm-from-image`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-image-2026-07-31/run.py:79`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-image-2026-07-31/results.json` |
| `azure-image-2026-07-31` | `B1.create-vm-attach-disk-no-image` | `B1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `create-vm-attach-disk-no-image`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-image-2026-07-31/run.py:97`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-image-2026-07-31/results.json` |
| `azure-k8s-synth-2026-07-31` | `L2.lb-rules-after-delete` | `L2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `lb-rules-after-delete`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-k8s-synth-2026-07-31/run.py:187`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-k8s-synth-2026-07-31/results.json` |
| `azure-k8s-synth-2026-07-31` | `L3.pips-after-delete` | `L3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `pips-after-delete`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-k8s-synth-2026-07-31/run.py:189`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-k8s-synth-2026-07-31/results.json` |
| `azure-k8s-synth-2026-07-31` | `L6.pvc-disks-after-delete` | `L6`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `pvc-disks-after-delete`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-k8s-synth-2026-07-31/run.py:207`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-k8s-synth-2026-07-31/results.json` |
| `azure-k8s-synth-2026-07-31` | `P2.pvc-status-alone` | `P2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `pvc-status-alone`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-k8s-synth-2026-07-31/run.py:122`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-k8s-synth-2026-07-31/results.json` |
| `azure-k8s-synth-2026-07-31` | `P3b.pvc-disks-alone` | `P3b`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `pvc-disks-alone`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-k8s-synth-2026-07-31/run.py:127`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-k8s-synth-2026-07-31/results.json` |
| `azure-k8s-synth-2026-07-31` | `P5.pvc-bound` | `P5`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `pvc-bound`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-k8s-synth-2026-07-31/run.py:139`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-k8s-synth-2026-07-31/results.json` |
| `azure-k8s-synth-2026-07-31` | `P6b.pvc-disks-after-pod` | `P6b`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `pvc-disks-after-pod`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-k8s-synth-2026-07-31/run.py:142`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-k8s-synth-2026-07-31/results.json` |
| `azure-k8s-synth-2026-07-31` | `P7.pv-volumehandle-hint` | `P7`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `pv-volumehandle-hint`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-k8s-synth-2026-07-31/run.py:143`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-k8s-synth-2026-07-31/results.json` |
| `azure-k8s-synth-2026-07-31` | `S3.svc-ingress-hint` | `S3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `svc-ingress-hint`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-k8s-synth-2026-07-31/run.py:163`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-k8s-synth-2026-07-31/results.json` |
| `azure-k8s-synth-2026-07-31` | `S4b.lb-after-svc-requery` | `S4b`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `lb-after-svc-requery`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-k8s-synth-2026-07-31/results.json:113`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/azure-k8s-synth-2026-07-31/results.json` |
| `azure-k8s-synth-2026-07-31` | `S5.pips-after-svc` | `S5`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `pips-after-svc`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-k8s-synth-2026-07-31/run.py:169`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-k8s-synth-2026-07-31/results.json` |
| `azure-k8s-synth2-2026-07-31` | `I3.ingress-address-hint` | `I3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `ingress-address-hint`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-k8s-synth2-2026-07-31/run.py:127`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-k8s-synth2-2026-07-31/results.json` |
| `azure-k8s-synth2-2026-07-31` | `I5.appgw-after-ingress` | `I5`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `appgw-after-ingress`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-k8s-synth2-2026-07-31/run.py:134`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-k8s-synth2-2026-07-31/results.json` |
| `azure-k8s-synth2-2026-07-31` | `K5.ingressclasses` | `K5`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `ingressclasses`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-k8s-synth2-2026-07-31/run.py:95`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-k8s-synth2-2026-07-31/results.json` |
| `azure-k8s-vpn-2026-07-31` | `K1.aks-omit-agentpools` | `K1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `aks-omit-agentpools`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `InvalidParameter` 오류를 돌려줬다 | `experiments/azure-k8s-vpn-2026-07-31/run.py:71`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-k8s-vpn-2026-07-31/results.json` |
| `azure-k8s-vpn-2026-07-31` | `V1.vng-wrong-subnet-name` | `V1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `vng-wrong-subnet-name`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `InvalidResourceReference` 오류를 돌려줬다 | `experiments/azure-k8s-vpn-2026-07-31/run.py:91`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-k8s-vpn-2026-07-31/results.json` |
| `azure-lb-serve2-2026-08-01` | `F1.lb-serves` | `F1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `lb-serves`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-lb-serve2-2026-08-01/run.py:196`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-lb-serve2-2026-08-01/results.json` |
| `azure-lb-serve2-2026-08-01` | `F1b.probe-hits-in-log` | `F1b`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `probe-hits-in-log`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-lb-serve2-2026-08-01/run.py:198`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-lb-serve2-2026-08-01/results.json` |
| `azure-lb-serve2-2026-08-01` | `F2.serving-lost` | `F2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `serving-lost`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-lb-serve2-2026-08-01/run.py:209`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-lb-serve2-2026-08-01/results.json` |
| `azure-lb-serve2-2026-08-01` | `F3.serving-again` | `F3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `serving-again`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-lb-serve2-2026-08-01/run.py:215`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-lb-serve2-2026-08-01/results.json` |
| `azure-lb-serve2-2026-08-01` | `M1.remove-nic-from-pool` | `M1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `remove-nic-from-pool`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-lb-serve2-2026-08-01/run.py:200`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-lb-serve2-2026-08-01/results.json` |
| `azure-lb-serve2-2026-08-01` | `M2.re-add-nic` | `M2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `re-add-nic`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-lb-serve2-2026-08-01/run.py:210`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-lb-serve2-2026-08-01/results.json` |
| `azure-preflight-2026-07-30` | `omit-lb-frontend-ref.validate` | `omit-lb-frontend-ref`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `validate`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `FrontendIPConfigurationHasNoSubnetOrPublicIPAddressOrPublicIPPrefix` 오류를 돌려줬다 | `experiments/azure-preflight-2026-07-30/results.json:102`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/azure-preflight-2026-07-30/results.json` |
| `azure-preflight-2026-07-30` | `omit-nic-subnet.validate` | `omit-nic-subnet`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `validate`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `SubnetIsRequired` 오류를 돌려줬다 | `experiments/azure-preflight-2026-07-30/results.json:132`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/azure-preflight-2026-07-30/results.json` |
| `azure-sig4-2026-07-31` | `F1b.resolve-works-nocache` | `F1b`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `resolve-works-nocache`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-sig4-2026-07-31/results.json:97`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/azure-sig4-2026-07-31/results.json` |
| `azure-sig4-2026-07-31` | `F1d.write-bare-works` | `F1d`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `write-bare-works`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-sig4-2026-07-31/results.json:233`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/azure-sig4-2026-07-31/results.json` |
| `azure-sig4-2026-07-31` | `F2b.resolve-lost-nocache` | `F2b`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `resolve-lost-nocache`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-sig4-2026-07-31/results.json:107`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/azure-sig4-2026-07-31/results.json` |
| `azure-sig4-2026-07-31` | `F2d.write-bare-lost` | `F2d`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `write-bare-lost`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-sig4-2026-07-31/results.json:243`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/azure-sig4-2026-07-31/results.json` |
| `azure-sig4-2026-07-31` | `F3b.resolve-again-nocache` | `F3b`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `resolve-again-nocache`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-sig4-2026-07-31/results.json:117`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/azure-sig4-2026-07-31/results.json` |
| `azure-sig4-2026-07-31` | `F3d.write-bare-again` | `F3d`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `write-bare-again`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-sig4-2026-07-31/results.json:262`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/azure-sig4-2026-07-31/results.json` |
| `azure-sig4-2026-07-31` | `M1b.delete-link-again` | `M1b`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-link-again`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-sig4-2026-07-31/results.json:102`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/azure-sig4-2026-07-31/results.json` |
| `azure-sig4-2026-07-31` | `M1d.detach-third` | `M1d`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `detach-third`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-sig4-2026-07-31/results.json:238`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/azure-sig4-2026-07-31/results.json` |
| `azure-sig4-2026-07-31` | `M2b.relink-again` | `M2b`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `relink-again`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-sig4-2026-07-31/results.json:112`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/azure-sig4-2026-07-31/results.json` |
| `azure-vpn2-2026-07-31` | `F2.delete-pip` | `F2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-pip`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-vpn2-2026-07-31/run.py:113`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-vpn2-2026-07-31/results.json` |
| `azure-vpn2-2026-07-31` | `F3.delete-vnet` | `F3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-vnet`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-vpn2-2026-07-31/run.py:115`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-vpn2-2026-07-31/results.json` |
| `azure-vpn2-2026-07-31` | `K0b.pip-zone-pair-constraint` | `K0b`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `pip-zone-pair-constraint`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `VmssVpnGatewayPublicIpsMustHaveZonesConfigured` 오류를 돌려줬다 | `experiments/azure-vpn2-2026-07-31/results.json:27`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/azure-vpn2-2026-07-31/results.json` |
| `azure-vpn2-2026-07-31` | `K1.create-vng-nowait` | `K1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `create-vng-nowait`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/azure-vpn2-2026-07-31/run.py:61`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-vpn2-2026-07-31/results.json` |
| `azure-vpn2-2026-07-31` | `L1.delete-gatewaysubnet-in-use` | `L1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-gatewaysubnet-in-use`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `InUseSubnetCannotBeDeleted` 오류를 돌려줬다 | `experiments/azure-vpn2-2026-07-31/run.py:89`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-vpn2-2026-07-31/results.json` |
| `azure-vpn2-2026-07-31` | `L2.delete-pip-in-use` | `L2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-pip-in-use`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `PublicIPAddressCannotBeDeleted` 오류를 돌려줬다 | `experiments/azure-vpn2-2026-07-31/run.py:92`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/azure-vpn2-2026-07-31/results.json` |
| `disj5-2026-08-01` | `D1.rest-both-source-and-initializeparams` | `D1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `rest-both-source-and-initializeparams`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `invalid` 오류를 돌려줬다 | `experiments/disj5-2026-08-01/run.py:101`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/disj5-2026-08-01/results.json` |
| `disj6-2026-08-01` | `B1.apply-both` | `B1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `apply-both`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `InvalidParameter` 오류를 돌려줬다 | `experiments/disj6-2026-08-01/run.py:138`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/disj6-2026-08-01/results.json` |
| `disj6-2026-08-01` | `B2.apply-image-only` | `B2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `apply-image-only`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/disj6-2026-08-01/run.py:139`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/disj6-2026-08-01/results.json` |
| `disj6-2026-08-01` | `B3.apply-disk-only` | `B3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `apply-disk-only`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/disj6-2026-08-01/run.py:140`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/disj6-2026-08-01/results.json` |
| `gcp-apply-2026-07-31` | `A.firewall-dangling-network` | `A`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `firewall-dangling-network`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `notFound` 오류를 돌려줬다 | `experiments/gcp-apply-2026-07-31/run.py:123`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-apply-2026-07-31/results.json` |
| `gcp-apply-2026-07-31` | `A.firewall-omit-network` | `A`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `firewall-omit-network`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-apply-2026-07-31/run.py:127`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-apply-2026-07-31/results.json` |
| `gcp-apply-2026-07-31` | `A.instance-omit-disks` | `A`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `instance-omit-disks`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `invalid` 오류를 돌려줬다 | `experiments/gcp-apply-2026-07-31/run.py:153`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-apply-2026-07-31/results.json` |
| `gcp-apply-2026-07-31` | `A.instance-omit-nic` | `A`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `instance-omit-nic`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `invalid` 오류를 돌려줬다 | `experiments/gcp-apply-2026-07-31/run.py:150`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-apply-2026-07-31/results.json` |
| `gcp-apply-2026-07-31` | `A.subnet-dangling-network` | `A`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `subnet-dangling-network`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `notFound` 오류를 돌려줬다 | `experiments/gcp-apply-2026-07-31/run.py:120`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-apply-2026-07-31/results.json` |
| `gcp-apply-2026-07-31` | `A.subnet-omit-network` | `A`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `subnet-omit-network`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `invalid` 오류를 돌려줬다 | `experiments/gcp-apply-2026-07-31/run.py:118`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-apply-2026-07-31/results.json` |
| `gcp-apply-2026-07-31` | `C.delete-bootdisk-attached` | `C`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-bootdisk-attached`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `resourceInUseByAnotherResource` 오류를 돌려줬다 | `experiments/gcp-apply-2026-07-31/run.py:167`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-apply-2026-07-31/results.json` |
| `gcp-apply-2026-07-31` | `C.delete-network-in-use` | `C`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-network-in-use`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `RESOURCE_IN_USE_BY_ANOTHER_RESOURCE` 오류를 돌려줬다 | `experiments/gcp-apply-2026-07-31/run.py:166`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-apply-2026-07-31/results.json` |
| `gcp-apply-2026-07-31` | `C.delete-subnet-in-use` | `C`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-subnet-in-use`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `resourceInUseByAnotherResource` 오류를 돌려줬다 | `experiments/gcp-apply-2026-07-31/run.py:165`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-apply-2026-07-31/results.json` |
| `gcp-apply-2026-07-31` | `D.delete-disk.depkbg-vm` | `D`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-disk.depkbg-vm`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-apply-2026-07-31/results.json:127`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/gcp-apply-2026-07-31/results.json` |
| `gcp-apply-2026-07-31` | `D.delete-network` | `D`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-network`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-apply-2026-07-31/run.py:185`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-apply-2026-07-31/results.json` |
| `gcp-apply-2026-07-31` | `D.delete-subnet` | `D`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-subnet`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-apply-2026-07-31/run.py:184`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-apply-2026-07-31/results.json` |
| `gcp-apply-2026-07-31` | `F0.create-instance` | `F0`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `create-instance`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-apply-2026-07-31/run.py:162`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-apply-2026-07-31/results.json` |
| `gcp-apply2-2026-07-31` | `A1.nic-network-only-custom` | `A1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `nic-network-only-custom`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `invalid` 오류를 돌려줬다 | `experiments/gcp-apply2-2026-07-31/run.py:121`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-apply2-2026-07-31/results.json` |
| `gcp-apply2-2026-07-31` | `A2.nic-network-only-auto` | `A2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `nic-network-only-auto`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-apply2-2026-07-31/run.py:125`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-apply2-2026-07-31/results.json` |
| `gcp-apply2-2026-07-31` | `A2.server-filled-subnetwork` | `A2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `server-filled-subnetwork`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-apply2-2026-07-31/run.py:131`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-apply2-2026-07-31/results.json` |
| `gcp-apply2-2026-07-31` | `B.create-ext-forwardingrule` | `B`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `create-ext-forwardingrule`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-apply2-2026-07-31/run.py:141`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-apply2-2026-07-31/results.json` |
| `gcp-apply3-2026-07-31` | `I1.internal-fr-omit-subnet` | `I1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `internal-fr-omit-subnet`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `invalid` 오류를 돌려줬다 | `experiments/gcp-apply3-2026-07-31/run.py:117`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-apply3-2026-07-31/results.json` |
| `gcp-apply3-2026-07-31` | `I2.internal-fr-full` | `I2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `internal-fr-full`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-apply3-2026-07-31/run.py:119`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-apply3-2026-07-31/results.json` |
| `gcp-apply4-2026-07-31` | `I3.internal-fr-omit-network` | `I3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `internal-fr-omit-network`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-apply4-2026-07-31/run.py:48`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-apply4-2026-07-31/results.json` |
| `gcp-apply4-2026-07-31` | `I3.server-filled-network` | `I3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `server-filled-network`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-apply4-2026-07-31/run.py:56`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-apply4-2026-07-31/results.json` |
| `gcp-cimg-2026-07-31` | `A1.dangling-source-disk` | `A1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `dangling-source-disk`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `notFound` 오류를 돌려줬다 | `experiments/gcp-cimg-2026-07-31/run.py:53`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-cimg-2026-07-31/results.json` |
| `gcp-cimg-2026-07-31` | `A3.image-ready` | `A3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `image-ready`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-cimg-2026-07-31/run.py:69`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-cimg-2026-07-31/results.json` |
| `gcp-cimg-2026-07-31` | `C1.create-vm-from-custom-image` | `C1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `create-vm-from-custom-image`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-cimg-2026-07-31/run.py:81`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-cimg-2026-07-31/results.json` |
| `gcp-cimg-2026-07-31` | `C2.vm-running` | `C2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `vm-running`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-cimg-2026-07-31/run.py:98`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-cimg-2026-07-31/results.json` |
| `gcp-dns-2026-07-31` | `A1.record-without-zone` | `A1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `record-without-zone`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `notFound` 오류를 돌려줬다 | `experiments/gcp-dns-2026-07-31/run.py:59`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-dns-2026-07-31/results.json` |
| `gcp-dns-2026-07-31` | `A3.create-record` | `A3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `create-record`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-dns-2026-07-31/run.py:69`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-dns-2026-07-31/results.json` |
| `gcp-dns-2026-07-31` | `L1.delete-zone-with-record` | `L1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-zone-with-record`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `containerNotEmpty` 오류를 돌려줬다 | `experiments/gcp-dns-2026-07-31/run.py:71`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-dns-2026-07-31/results.json` |
| `gcp-dns-2026-07-31` | `L3.delete-zone-after` | `L3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-zone-after`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-dns-2026-07-31/run.py:75`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-dns-2026-07-31/results.json` |
| `gcp-fs-2026-07-31` | `A1.omit-network` | `A1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `omit-network`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `INVALID_ARGUMENT` 오류를 돌려줬다 | `experiments/gcp-fs-2026-07-31/run.py:60`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-fs-2026-07-31/results.json` |
| `gcp-fs-2026-07-31` | `A2.dangling-network` | `A2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `dangling-network`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `INVALID_ARGUMENT` 오류를 돌려줬다 | `experiments/gcp-fs-2026-07-31/run.py:63`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-fs-2026-07-31/results.json` |
| `gcp-func-2026-07-31` | `F1.reachable-baseline` | `F1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `reachable-baseline`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-func-2026-07-31/run.py:101`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-func-2026-07-31/results.json` |
| `gcp-func-2026-07-31` | `F2.unreachable-after-delete` | `F2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `unreachable-after-delete`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-func-2026-07-31/run.py:110`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-func-2026-07-31/results.json` |
| `gcp-func-2026-07-31` | `F3.reachable-again` | `F3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `reachable-again`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-func-2026-07-31/run.py:120`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-func-2026-07-31/results.json` |
| `gcp-func-2026-07-31` | `M1.delete-accessconfig-while-running` | `M1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-accessconfig-while-running`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-func-2026-07-31/run.py:103`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-func-2026-07-31/results.json` |
| `gcp-func-2026-07-31` | `M1b.vm-still-running` | `M1b`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `vm-still-running`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-func-2026-07-31/run.py:107`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-func-2026-07-31/results.json` |
| `gcp-func-2026-07-31` | `M2.add-accessconfig` | `M2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `add-accessconfig`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-func-2026-07-31/run.py:112`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-func-2026-07-31/results.json` |
| `gcp-func2-2026-07-31` | `F1.reachable-baseline` | `F1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `reachable-baseline`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-func2-2026-07-31/run.py:111`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-func2-2026-07-31/results.json` |
| `gcp-func2-2026-07-31` | `F2.unreachable-no-rule` | `F2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `unreachable-no-rule`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-func2-2026-07-31/run.py:115`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-func2-2026-07-31/results.json` |
| `gcp-func2-2026-07-31` | `F3.reachable-again` | `F3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `reachable-again`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-func2-2026-07-31/run.py:117`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-func2-2026-07-31/results.json` |
| `gcp-func2-2026-07-31` | `F4.unreachable-no-route` | `F4`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `unreachable-no-route`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-func2-2026-07-31/run.py:130`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-func2-2026-07-31/results.json` |
| `gcp-func2-2026-07-31` | `F5.reachable-final` | `F5`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `reachable-final`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-func2-2026-07-31/run.py:134`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-func2-2026-07-31/results.json` |
| `gcp-func2-2026-07-31` | `M1.delete-fw-rule` | `M1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-fw-rule`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-func2-2026-07-31/run.py:113`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-func2-2026-07-31/results.json` |
| `gcp-func2-2026-07-31` | `M2.recreate-fw-rule` | `M2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `recreate-fw-rule`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-func2-2026-07-31/run.py:116`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-func2-2026-07-31/results.json` |
| `gcp-func2-2026-07-31` | `M3a2.find-default-route-clientside` | `M3a2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `find-default-route-clientside`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-func2-2026-07-31/results.json:69`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/gcp-func2-2026-07-31/results.json` |
| `gcp-func2-2026-07-31` | `M3b.delete-default-route` | `M3b`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-default-route`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-func2-2026-07-31/run.py:128`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-func2-2026-07-31/results.json` |
| `gcp-func2-2026-07-31` | `M4.recreate-route` | `M4`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `recreate-route`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-func2-2026-07-31/run.py:131`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-func2-2026-07-31/results.json` |
| `gcp-gke2-2026-07-31` | `G1.create-omit-network` | `G1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `create-omit-network`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-gke2-2026-07-31/run.py:62`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-gke2-2026-07-31/results.json` |
| `gcp-gke2-2026-07-31` | `G2.status-final` | `G2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `status-final`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-gke2-2026-07-31/run.py:78`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-gke2-2026-07-31/results.json` |
| `gcp-gke2-2026-07-31` | `G3.server-filled-network` | `G3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `server-filled-network`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-gke2-2026-07-31/run.py:83`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-gke2-2026-07-31/results.json` |
| `gcp-gke2-2026-07-31` | `P1.nodepool-add` | `P1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `nodepool-add`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-gke2-2026-07-31/run.py:93`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-gke2-2026-07-31/results.json` |
| `gcp-gke2-2026-07-31` | `P3.nodepool-delete-retry` | `P3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `nodepool-delete-retry`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-gke2-2026-07-31/run.py:118`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-gke2-2026-07-31/results.json` |
| `gcp-gke3-2026-07-31` | `F2.delete-subnet` | `F2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-subnet`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-gke3-2026-07-31/run.py:110`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-gke3-2026-07-31/results.json` |
| `gcp-gke3-2026-07-31` | `F3.delete-network` | `F3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-network`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-gke3-2026-07-31/run.py:111`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-gke3-2026-07-31/results.json` |
| `gcp-gke3-2026-07-31` | `L1.delete-subnet-in-use` | `L1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-subnet-in-use`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `resourceInUseByAnotherResource` 오류를 돌려줬다 | `experiments/gcp-gke3-2026-07-31/run.py:93`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-gke3-2026-07-31/results.json` |
| `gcp-gke3-2026-07-31` | `L2.delete-network-in-use` | `L2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-network-in-use`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `RESOURCE_IN_USE_BY_ANOTHER_RESOURCE` 오류를 돌려줬다 | `experiments/gcp-gke3-2026-07-31/run.py:94`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-gke3-2026-07-31/results.json` |
| `gcp-iam-2026-07-31` | `A1.create-vm-omit-sa` | `A1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `create-vm-omit-sa`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-iam-2026-07-31/run.py:45`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-iam-2026-07-31/results.json` |
| `gcp-iam-2026-07-31` | `A2.running` | `A2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `running`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-iam-2026-07-31/run.py:62`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-iam-2026-07-31/results.json` |
| `gcp-iam-2026-07-31` | `A3.serviceaccounts-shape` | `A3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `serviceaccounts-shape`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-iam-2026-07-31/run.py:66`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-iam-2026-07-31/results.json` |
| `gcp-image-2026-07-31` | `G1.omit-image-and-source` | `G1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `omit-image-and-source`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `invalid` 오류를 돌려줬다 | `experiments/gcp-image-2026-07-31/run.py:57`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-image-2026-07-31/results.json` |
| `gcp-image-2026-07-31` | `G2.dangling-image` | `G2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `dangling-image`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `invalid` 오류를 돌려줬다 | `experiments/gcp-image-2026-07-31/run.py:62`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-image-2026-07-31/results.json` |
| `gcp-image-2026-07-31` | `G3b.boot-from-existing-disk-no-image` | `G3b`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `boot-from-existing-disk-no-image`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-image-2026-07-31/run.py:74`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-image-2026-07-31/results.json` |
| `gcp-image-2026-07-31` | `G3c.instance-shape` | `G3c`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `instance-shape`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-image-2026-07-31/run.py:79`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-image-2026-07-31/results.json` |
| `gcp-k8s-synth-2026-07-31` | `L2.lb-after-delete` | `L2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `lb-after-delete`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-k8s-synth-2026-07-31/run.py:202`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-k8s-synth-2026-07-31/results.json` |
| `gcp-k8s-synth-2026-07-31` | `L3.fw-after-delete` | `L3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `fw-after-delete`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-k8s-synth-2026-07-31/run.py:204`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-k8s-synth-2026-07-31/results.json` |
| `gcp-k8s-synth-2026-07-31` | `L6.pvc-disks-after-delete` | `L6`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `pvc-disks-after-delete`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-k8s-synth-2026-07-31/run.py:219`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-k8s-synth-2026-07-31/results.json` |
| `gcp-k8s-synth-2026-07-31` | `P2.pvc-status-alone` | `P2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `pvc-status-alone`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-k8s-synth-2026-07-31/run.py:152`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-k8s-synth-2026-07-31/results.json` |
| `gcp-k8s-synth-2026-07-31` | `P3.disks-after-pvc-alone` | `P3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `disks-after-pvc-alone`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-k8s-synth-2026-07-31/run.py:155`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-k8s-synth-2026-07-31/results.json` |
| `gcp-k8s-synth-2026-07-31` | `P5.pvc-bound` | `P5`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `pvc-bound`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-k8s-synth-2026-07-31/run.py:167`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-k8s-synth-2026-07-31/results.json` |
| `gcp-k8s-synth-2026-07-31` | `P6.disks-after-pod` | `P6`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `disks-after-pod`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-k8s-synth-2026-07-31/run.py:169`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-k8s-synth-2026-07-31/results.json` |
| `gcp-k8s-synth-2026-07-31` | `P7.pv-volumehandle-hint` | `P7`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `pv-volumehandle-hint`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-k8s-synth-2026-07-31/run.py:170`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-k8s-synth-2026-07-31/results.json` |
| `gcp-k8s-synth-2026-07-31` | `S2.svc-ingress-hint` | `S2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `svc-ingress-hint`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-k8s-synth-2026-07-31/run.py:186`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-k8s-synth-2026-07-31/results.json` |
| `gcp-k8s-synth-2026-07-31` | `S3.lb-after-svc` | `S3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `lb-after-svc`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-k8s-synth-2026-07-31/run.py:188`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-k8s-synth-2026-07-31/results.json` |
| `gcp-k8s-synth2-2026-07-31` | `I3.ingress-address-hint` | `I3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `ingress-address-hint`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-k8s-synth2-2026-07-31/run.py:155`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-k8s-synth2-2026-07-31/results.json` |
| `gcp-k8s-synth2-2026-07-31` | `I4.http-lb-after-ingress` | `I4`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `http-lb-after-ingress`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-k8s-synth2-2026-07-31/run.py:157`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-k8s-synth2-2026-07-31/results.json` |
| `gcp-k8s-synth2-2026-07-31` | `I5.ingress-events-hint` | `I5`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `ingress-events-hint`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-k8s-synth2-2026-07-31/run.py:158`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-k8s-synth2-2026-07-31/results.json` |
| `gcp-k8s-synth2-2026-07-31` | `I7.http-lb-after-delete` | `I7`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `http-lb-after-delete`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-k8s-synth2-2026-07-31/run.py:172`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-k8s-synth2-2026-07-31/results.json` |
| `gcp-paircompat-2026-07-31` | `P1.zone-mismatch-vm-zoneB` | `P1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `zone-mismatch-vm-zoneB`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `invalid` 오류를 돌려줬다 | `experiments/gcp-paircompat-2026-07-31/run.py:53`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-paircompat-2026-07-31/results.json` |
| `gcp-paircompat-2026-07-31` | `P2.same-zone-control` | `P2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `same-zone-control`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-paircompat-2026-07-31/run.py:56`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-paircompat-2026-07-31/results.json` |
| `gcp-svcdisc-2026-07-31` | `F1.discovery-works` | `F1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `discovery-works`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-svcdisc-2026-07-31/run.py:160`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-svcdisc-2026-07-31/results.json` |
| `gcp-svcdisc-2026-07-31` | `F2.discovery-lost` | `F2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `discovery-lost`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-svcdisc-2026-07-31/run.py:166`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-svcdisc-2026-07-31/results.json` |
| `gcp-svcdisc-2026-07-31` | `F3.discovery-again` | `F3`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `discovery-again`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-svcdisc-2026-07-31/run.py:170`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-svcdisc-2026-07-31/results.json` |
| `gcp-svcdisc-2026-07-31` | `M1.delete-service` | `M1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-service`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-svcdisc-2026-07-31/run.py:162`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-svcdisc-2026-07-31/results.json` |
| `gcp-svcdisc-2026-07-31` | `M1b.pods-still-running` | `M1b`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `pods-still-running`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-svcdisc-2026-07-31/run.py:163`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-svcdisc-2026-07-31/results.json` |
| `gcp-svcdisc-2026-07-31` | `M2.recreate-service` | `M2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `recreate-service`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-svcdisc-2026-07-31/run.py:168`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-svcdisc-2026-07-31/results.json` |
| `gcp-vpn-fs-2026-07-31` | `F10.ready-b` | `F10`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `ready-b`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-vpn-fs-2026-07-31/results.json:133`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/gcp-vpn-fs-2026-07-31/results.json` |
| `gcp-vpn-fs-2026-07-31` | `F9.create-b` | `F9`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `create-b`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-vpn-fs-2026-07-31/results.json:127`<br>결과 JSON에만 남은 이름 — 이 저장소에서 원래 요청 코드는 찾지 못함 | `experiments/gcp-vpn-fs-2026-07-31/results.json` |
| `gcp-vpn-fs-2026-07-31` | `L1.delete-network-with-gateway` | `L1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-network-with-gateway`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `RESOURCE_IN_USE_BY_ANOTHER_RESOURCE` 오류를 돌려줬다 | `experiments/gcp-vpn-fs-2026-07-31/run.py:72`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-vpn-fs-2026-07-31/results.json` |
| `gcp-vpn-fs-2026-07-31` | `T2.delete-network` | `T2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `delete-network`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-vpn-fs-2026-07-31/run.py:113`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-vpn-fs-2026-07-31/results.json` |
| `gcp-vpn-fs-2026-07-31` | `V1.gateway-without-network` | `V1`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `gateway-without-network`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계에서 CSP가 `invalid` 오류를 돌려줬다 | `experiments/gcp-vpn-fs-2026-07-31/run.py:64`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-vpn-fs-2026-07-31/results.json` |
| `gcp-vpn-fs-2026-07-31` | `V2.create-gateway` | `V2`은 이 실험 안에서만 쓰는 묶음·순번이고 전역 표준이 아니다. `create-gateway`은 시험의 짧은 이름이다. 실제 요청과 값은 정의 코드를 따른다. | 이 단계가 성공했다 | `experiments/gcp-vpn-fs-2026-07-31/run.py:68`<br>이 저장소의 실험 코드에서 정의한 이름 | `experiments/gcp-vpn-fs-2026-07-31/results.json` |

## 7. 읽을 때 지켜야 할 경계

- `required`는 **그 claim이 다루는 생성 경로**에서만 필수라는 뜻이다. 모든 배포 방식에서의 보편 법칙이 아니다.
- `optional`은 생성 가능 여부의 결론이다. 보안·외부 접속·서비스 정상 동작까지 선택이라는 뜻은 아니다.
- `holds`는 lifecycle/function 질문에서 관계를 관측했다는 뜻이다. `required`의 동의어가 아니다.
- `resultFile`의 오류 문자열은 CSP가 준 원문이다. 이해하기 어려운 원문이라도 삭제하지 않는다. 이 문서의 설명과 `note`가 먼저 읽을 층이고, 원문은 검증할 때 본다.
- `definition`은 우리 실험 코드다. 따라서 이름 자체는 우리 것이고, 그 실행 결과·오류 코드는 CSP의 것이다. 둘을 구별해야 한다.
