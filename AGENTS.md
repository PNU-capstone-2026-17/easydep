# EasyDep 작업 지침

## 외부 네트워크 실행

- LLM API, CSP CLI, 패키지 저장소 등 외부 네트워크 사용이 명백한 작업은 첫 시도부터 네트워크 권한이 필요한 실행으로 요청한다.
- 네트워크가 차단된 기본 샌드박스에서 먼저 실행하여 예측 가능한 연결 실패를 만들지 않는다.
- `APIConnectionError`를 실제 엔드포인트 장애로 판단하기 전에 샌드박스의 네트워크 차단 여부를 구분한다.
- 429 응답, 스트림 미종료, 실제 엔드포인트 타임아웃은 샌드박스 차단과 별개의 원인으로 기록한다.

## 실패 후 재개

- 환경만 바뀌었거나 한 단계만 실패한 경우에는 전체 실행을 처음부터 반복하지 않는다.
- 가능한 경우 기존 체크포인트와 산출물을 검증한 뒤 실패한 단계 또는 하위 작업부터 재개한다.
- 체크포인트를 재사용할 때에는 실행 ID, 앱 ID, 완료 단계 및 산출물 대응이 일치하는지 확인한다.

## Windows UTF-8 실행

- Windows에서 Python으로 JSON, LLM 응답, 다이어그램 텍스트처럼 비ASCII 문자가 포함될 수 있는 값을 표준 출력하거나 파일로 읽고 쓸 때에는 첫 실행부터 UTF-8 모드를 사용한다. Python은 `-X utf8`로 실행하거나 `PYTHONUTF8=1`을 명시한다.
- PowerShell의 `Get-Content`, `Set-Content`, `Out-File`로 해당 산출물을 다룰 때에는 `-Encoding UTF8`을 명시한다. 기본 CP949에 의존하지 않는다.
- 산출물 저장 뒤 콘솔 출력에서만 `UnicodeEncodeError`가 발생했다면 전체 단계를 재실행하지 않는다. 저장된 산출물의 존재와 완결성을 먼저 확인하고 실패한 출력·요약 작업만 UTF-8로 재개한다.

## 임시 파일과 브라우저 프로필

- Chrome·Edge 등 브라우저 자동화의 `user-data-dir`이나 패키지 캐시처럼 파일을 대량 생성하는 경로를 저장소 안에 만들지 않는다.
- 임시 실행이 필요하면 시스템 임시 디렉터리 아래에 작업별 단일 디렉터리를 만들고, 재시도할 때 새 디렉터리를 계속 늘리지 말고 안전한 경우 기존 디렉터리를 재사용한다.
- 실행 전 예상 생성량과 대상 경로를 확인하고, 실행이 끝나거나 실패하면 해당 작업이 만든 프로세스와 임시 디렉터리만 정리한 뒤 잔여 경로가 0개인지 확인한다.
- 저장소 안에서 임시 파일이 불가피하면 고유 접두사를 사용하고, 다른 사용자 파일과 구분되는 정확한 경로만 제거한다.
- 화면 검증을 반복할 때에는 매번 전체 브라우저 프로필을 만들지 말고, 가능한 한 한 프로필·한 프로세스에서 여러 화면을 검사하거나 DOM·구문 검증 같은 가벼운 방법을 우선한다.
