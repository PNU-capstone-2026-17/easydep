# torch는 CPU 전용 휠을 사용 (CUDA 빌드 대비 이미지 대폭 축소)
--extra-index-url https://download.pytorch.org/whl/cpu

# --- 에이전트 공통 ---
langgraph==1.2.8
fastapi==0.125.0
uvicorn[standard]==0.38.0
python-dotenv==1.2.2
PyYAML>=6.0
jsonschema>=4.20

# --- 요구사항 분석 에이전트 ---
# LLM 호출은 langchain-openai(구조화 출력 with_structured_output)를 쓴다.
langchain-openai>=0.2.0
langchain-core>=0.3.0
pydantic-settings>=2.5.0
# step1_requirements → legacy.auto_clarify → legacy.example_sampler 가 쓴다.
# 목록에 없어서 requirements.txt 만으로는 임포트조차 안 됐다.
pandas>=2.0
# 요구사항 FR/NFR BERT 검증 분류기 (materials의 파인튜닝 모델 로드)
torch>=2.2.0
transformers>=4.44.0
safetensors>=0.4.0

# --- 시스템 설계 에이전트 ---
# 설계 쪽은 OpenAI 호환 클라이언트를 직접 호출한다.
# 2.44.0 고정이었으나 openai-agents(배포 에이전트)가 >=2.45.0을 요구해 올렸다.
openai>=2.45.0,<3
# MySQL 산출물 저장소
sqlalchemy==2.0.44
# 1.4.6 으로 고정돼 있었으나 PyMySQL에 그런 버전이 없다(최신이 1.2.0). 이 핀 때문에
# requirements.txt 가 통째로 설치 불가였다 — 두 에이전트를 합친 3015778 이후 계속.
pymysql==1.2.0

# --- 시스템 구현 에이전트(OpenHands SDK 기반 restricted editor worker) ---
openhands-sdk==1.36.1
openhands-tools==1.36.1
# openhands-sdk 가 litellm>=1.84.1 만 요구해서 최신(1.93.0)을 잡는데, 그 버전은
# 휠 없이 sdist 만 올라와 있어 Rust 툴체인을 요구하며 설치가 실패한다.
# 휠이 있는 마지막 계열로 막는다.
litellm<1.92

# --- 배포 에이전트(클라우드 지식베이스) ---
# 산출물은 app/deployment/data 에 커밋돼 있어 읽기 경로에는 빌드가 필요 없다.
openai-agents>=0.18.2
httpx>=0.28
pyyaml>=6
jsonschema>=4.23
# 읽기 경로 전용 가속. jsonschema(순수 파이썬)가 세션 첫 로드의 94~96%를 먹었고
# (costkb 28.8초·perfkb 48.6초) 스키마를 코드로 컴파일하면 18배 빠르다.
fastjsonschema>=2.19
# 표준 json은 순수 파이썬이라 28MB에 1.9초, orjson은 0.58초 (실측).
orjson>=3.10
# 벤더 스키마의 정규식이 .NET/PCRE 문법이라 파이썬 re가 205건을 못 읽는다.
regex>=2024.5.15
ddgs>=9.14.4

# 로컬 빌드 전용이라 넣지 않는 것: neo4j·pgdumplib(costkb/perfkb 덤프)·pypdf.
# 필요해지면 `pip install neo4j pgdumplib pypdf`로 각자 넣는다.
